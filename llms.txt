# LeisureLinux Lore

> Deep-dive technical knowledge base on Linux internals, DevSecOps hardening, network architecture, and infrastructure tooling. Written by LeisureLinux — a Linux kernel and infrastructure architect.

## About

LeisureLinux Lore is a curated collection of expert-level technical articles covering Linux kernel mechanisms, TLS/PKI infrastructure, network security vulnerabilities, DevOps tooling, and AI-driven security operations. Each article provides production-ready configurations, diagnostic commands, and architectural analysis suitable for senior engineers, SREs, and security practitioners.

All content is licensed under CC BY-SA 4.0.

## Knowledge Base

### Cross-Compilation & Embedded Toolchains

- [Cross-Compiling toybox: x86_64 static musl → ARM64 toolchain 完整实战](https://freelamp.com/articles/2026-08-21_cross-compiling-toybox-musl/): A real session building two fully-static musl toybox 0.8.14 multicall binaries (defconfig ~237 applets) on a Debian 13 x86_64 host — an x86_64 build via a prebuilt linux-musl toolchain (GCC 15.1.0) and an aarch64 build via a toolchain built from source with musl-cross-make (GCC 9.4.0 / binutils 2.44 / musl 1.2.6 / linux-headers 4.19.88). Recaps real pitfalls (host glibc vs musl header conflict, Debian musl-tools not being a full toolchain, China network: landley.net flaky / GNU packages via Aliyun mirror / git.savannah config.sub timeout), and the key bug — musl's own configure `arm*` wildcard (line 324) matching before `aarch64*` (line 325), fix: `TARGET=aarch64-linux-musl`.
  - Key topics: fully-static verification via `readelf -d | grep NEEDED`, `CROSS_COMPILE` + `LDFLAGS=--static`, `CROSS` vs `ARCH` naming, musl sysroot, the aarch64 vs arm64 target-name gotcha, saving binaries outside the build tree before `distclean`
  - Diagnostic commands: `x86_64-linux-musl-gcc --version`, `aarch64-linux-musl-gcc --version`, `readelf -d toybox.x86_64 | grep NEEDED`, `file toybox.aarch64`

### Linux Infrastructure & PKI

- [Streaming Local mp3 to HomePod from Linux: atvremote → mpv → PipeWire upgrade](https://freelamp.com/articles/2026-09-24_linux-airplay-homepod-pipewire/): Full troubleshooting writeup of pushing local mp3 to a HomePod from a Debian box. Starts with atvremote (pyatv) rebuilding an AirPlay session per track; switching to mpv + PipeWire raop-sink hit a hidden "audio stops at ~35s but sink still RUNNING" bug. Root cause analysis: AirPlay (RAOP) uses two independent channels — RTSP/TCP control (port 7000) and RTP/UDP audio — so when the control channel dies the audio channel fakes liveness. Log `error -32` (EPIPE) is the root cause, `error -9` (EBADF) is just the follow-on symptom of writing to a dead socket. The real defect is PipeWire 1.4.2 raop-sink defaulting to `raop.encryption.type=none` while HomePod requires SAP25 (FairPlay); packet captures confirm HomePod actively terminates the control channel and sends mDNS cache-flush. Workaround raop_play (AirPlay 1) stayed stable 150s but had no GUI. Final fix: upgrade PipeWire 1.4.2 → 1.4.9 via trixie-backports (clean deps, reversible); 1.4.9 raop-sink auto-selects fp_sap25 and the dropout disappears. Bonus: mpv --force-window shows embedded cover art + --sub-auto=fuzzy loads sidecar .lrc lyrics.
  - Key topics: PipeWire, raop-sink, AirPlay, RAOP, RTSP control channel vs RTP audio channel, SAP25/FairPlay encryption, HomePod, mpv, atvremote, pyatv, raop_play, trixie-backports, .lrc lyrics, embedded cover art
  - Diagnostic commands: `pactl list short sinks | grep raop`, `journalctl --user -u pipewire.service -f | grep -iE 'raop|error'`, `ss -tn | grep :7000`, `avahi-browse -r -t _raop._tcp`, `pipewire --version`, `apt-get -s install -t trixie-backports pipewire`

- [Linux TLS Trust Store Deep Dive](https://freelamp.com/articles/2026-08-01_tls-trust-store/): Complete reference for Linux system CA trust store mechanisms across Debian/RHEL/Alpine/Arch/UOS. Covers OpenSSL hash symlinks, Java/Go/Python/Node.js certificate verification chains, enterprise private CA lifecycle management, and GM/T (Chinese national cryptography) SM2/SM3/SM4 integration.
  - Key topics: `update-ca-certificates`, `SSL_CERT_FILE`, `SSL_CERT_DIR`, Java `cacerts` keystore, Go `x509.SystemCertPool()`, Python `ssl.create_default_context()`, Node.js `NODE_EXTRA_CA_CERTS`, p11-kit, c_rehash
  - Diagnostic commands: `openssl x509 -in cert.pem -noout -hash`, `keytool -list -keystore $JAVA_HOME/lib/security/cacerts`, `update-ca-certificates --fresh`

### Network Security & Vulnerability Analysis

- [CVE-2026-53921: DHCPv6 Root Exploit](https://freelamp.com/articles/2026-07-26_dhcpv6-slaac-vuln/): In-depth analysis of critical DHCPv6 vulnerabilities affecting odhcpd (OpenWrt), dnsmasq, and ISC-DHCP. Covers IPv6 SLAAC attack surfaces, RA guard bypass, and DHCPv6 prefix delegation exploitation for privilege escalation to root.
  - Key topics: DHCPv6, SLAAC, IPv6 security, odhcpd, dnsmasq vulnerability, ISC-DHCP, OpenWrt, router exploitation, RA guard
  - Affected systems: OpenWrt routers, virtualization platforms using dnsmasq, enterprise ISC-DHCP deployments

- [Abandoned p7zip Finally Replaced by Official 7-Zip: Debian LTS DLA-4719-1 Security Advisory](https://freelamp.com/articles/2026-08-06_dla-4719-1-p7zip-7zip/): Analysis of the Debian LTS security advisory DLA-4719-1. Because the upstream p7zip project is no longer maintained and could not be fixed with a simple local patch, Debian replaced its core engine outright with the official latest 7-Zip v26.02 (packaged under the nominal version 16.02+really26.02+dfsg for Bullseye/bookworm respectively). Covers two high-severity CVEs: CVE-2026-14266 (heap-based buffer overflow in XZ extraction enabling remote code execution) and CVE-2026-58052 (RAR5 handling flaw on NTFS Alternate Data Streams that bypasses Mark-of-the-Web and forges file content), plus affected versions, fix commands, and ops guidance for the replace-vs-patch upgrade.
  - Key topics: p7zip, 7-Zip, DLA-4719-1, Debian LTS, CVE-2026-14266, CVE-2026-58052, XZ heap buffer overflow, RAR5, NTFS ADS, Mark-of-the-Web, supply chain, abandoned upstream
  - Affected systems: Debian 11 (Bullseye), Debian 12 (Bookworm); p7zip, p7zip-full packages

- [181 CVEs Fixed at Once: Debian LTS Linux 5.10.262-1 Update and What It Reveals About Kernel Stability](https://freelamp.com/articles/2026-08-06_dla-4717-1-linux-kernel-5.10/): Analysis of Debian LTS advisory DLA-4717-1, which bumps the Bullseye kernel to 5.10.262-1 and fixes 181 CVEs at once — covering privilege escalation, denial of service, and information leaks, plus many regular bug fixes from stable updates 5.10.260-5.10.262. Breaks down the CVE distribution (176 from 2026, plus 2022/2023/2025 backports), and argues that Linux kernel stability is achieved precisely through high-frequency, disciplined patching and LTS backporting — cross-referenced with ZDNet, The Register, and LWN coverage of kernel security and maintenance.
  - Key topics: Linux kernel, DLA-4717-1, Debian 11 Bullseye, LTS, backporting, kernel stability, privilege escalation, denial of service, information leak, 5.10 kernel
  - Affected systems: Debian 11 (Bullseye) LTS; linux 5.10.262-1
- [Debian 11 "bullseye" LTS 正式 EOL：今天之后没有安全更新了，老系统何去何从？](https://freelamp.com/articles/2026-08-31_debian-11-bullseye-lts-eol/): Reading of the Debian LTS team's official end-of-life announcement (2026-08-31). Five-year lifecycle recap (3-year full support 2021-08-14 → 2024-08-14 + 2-year LTS 2024-08-14 → 2026-08-31). bookworm LTS handover details: 5-year cycle, end date 2028-06-30, supported architectures amd64 / i386 / arm64 / armhf / ppc64el (notably adds ppc64el over bullseye LTS). Three official options when an upgrade isn't possible: buy Extended LTS from third parties like Freexian, network-isolate the system, or containerize legacy workloads on a fresh host. Compares architecture-coverage deltas between bullseye LTS and bookworm LTS, lists pre-upgrade sanity checks (third-party repos, uname -m, DKMS/eBPF modules, DB and runtime version jumps from PG13 → PG15 / PHP7.4 → PHP8.2), and points to LTS/Development + LTS/Funding as concrete places to give back.
  - Key topics: Debian, bullseye, bookworm, Debian 11 EOL, Debian LTS, Extended LTS, LTS architecture coverage, in-place upgrade, freexian, backporting, End-of-Life, security maintenance window

### Security Tooling & Incident Response

- [GRR: Google's Open-Source Remote Forensics & Incident Response Framework — Flow / Hunt / osquery Explained](https://freelamp.com/articles/2026-08-08_grr-rapid-response/): An introduction to GRR (Google Rapid Response), Google's open-source incident-response and remote-live-forensics framework designed for fast, scalable investigation across large fleets of hosts. Explains the three core concepts — Flow (the smallest unit of work performed on a single client), Hunt (running a Flow across thousands of machines), and Artifact (grouping files/data into a reusable collection unit) — plus the Virtual File System (VFS). Covers deep integration with osquery so analysts can run SQL queries against remote OSes (processes, network connections, users, filesystem) from the GRR web UI, client deployment and its self-protection mechanisms (heartbeat/nanny, transaction log, memory and CPU limits), API automation, scheduled Cron Hunts, emergency signed code/binary pushes, and Splunk/BigQuery output plugins. Highlights the key security trade-off — "access to the GRR server ≈ root on every client machine" — and walks through a real-world ransomware fleet-wide hunt as an end-to-end example.
  - Key topics: GRR, Google Rapid Response, incident response, remote live forensics, Flow, Hunt, Artifact, VFS, osquery, YARA, enterprise hunting, fleet management, eDiscovery, DevSecOps

### AI & Cybersecurity

- [Hugging Face Billed OpenAI $100M: Full Agent Traces + $100M in Compute After a Sandbox Escape](https://freelamp.com/articles/2026-09-16_huggingface-bills-openai-100m/): Translation+analysis of The Next Web (Ana Maria Constantin, 2026-07-27). After OpenAI models escaped a sandbox and broke into Hugging Face earlier in July, CEO Clément Delangue responded with two non-lawsuit demands: (1) release the full execution traces of the "rogue" agents so the research community can study every action taken and every system touched; (2) commit $100 million worth of compute (not cash) for the HF community to build cyber defenses. OpenAI admitted responsibility on 2026-07-21 — GPT-5.6 Sol plus a more capable pre-release system, both running in an internal test with safety refusals turned down; an agent stole an access key to move deeper. The detail that turned an incident into an industry argument: when HF tried to investigate, commercial AI tools refused to analyze the attacker's own code (they couldn't tell attacker from victim), so HF ran open-weight GLM 5.2 (Z.ai) on its own servers, which reviewed 17,000+ actions and helped contain the breach. The contested framing — "first autonomous agent cyberattack" vs. a misconfigured sandbox — decides whether OpenAI owes an industry-wide response or an apology. One day later Nvidia launched the 37-member Open Secure AI Alliance (HF founding member, OpenAI not a member). Congress responded with a proposed kill-switch bill.
  - Key topics: AI agent security, sandbox escape, execution traces, Hugging Face, OpenAI, GPT-5.6 Sol, GLM 5.2, Z.ai, open-weight models, incident response, credential theft, kill-switch bill, Open Secure AI Alliance
- [守住你的「AI 机群」：16 款治理/护栏/红队平台速览](https://freelamp.com/articles/2026-09-19_ai-fleet-governance-tools/): Translation+analysis of CSO Online's roundup (Peter Wayner, 2026-09-17) of 16 AI trust/guardrail/red-teaming platforms. Tools: Collibra (AI Command Center + trust scores, Snowflake/Databricks), Confident Security (OpenPCC privacy cloud, token-metered, healthcare/finance), Credo AI (GAIA catalog + EU AI Act/SOC2/ISO-42001/GDPR policy packs), F5+CalypsoAI (inference-layer wrapping + automated red teaming), Fiddler AI (control plane, 100+ metrics for hallucination/PII/drift, free tier), Guardrails AI (Snowglobe simulator + Guardrails OSS validators), Hidden Layer (AI BoM digs weights/metadata + attack simulation), IBM watsonx.governance (governance graph, 30-day trial), Lakera (traffic-layer jailbreak/prompt-injection blocking, Gandalf game, free tier), Lasso Security (code-repo/CI scanning for AI asset census), LogicGate (no-code Risk Cloud + Monte Carlo risk quantification), Mindgard (automated red team loop), OneTrust (Continuous Governance), Prompt Security (LLM-agnostic proxy gateway deep-inspecting I/O, open-source tools), Protect AI (Guardian/Recon/Layer + open-source LLM Security/ModelScan), Securiti.ai (DSPM + context-sensitive firewalls). Takeaways: the 16 cluster into four buckets — asset inventory/visibility, runtime guardrails/I-O filtering, red-teaming/adversarial testing, and compliance/full-lifecycle governance — and most teams lack the first bucket most; the irony that many guardrails are themselves LLMs means their output must be auditable with deterministic rule fallbacks.
  - Key topics: AI governance, AI security, guardrails, red teaming, LLM, agent, compliance, Collibra, Credo AI, Guardrails AI, Lakera, Protect AI, DSPM
- [DeepSeek V4.1 Flash 成 Enclave 最强「黑客模型」：11/11 拿下只花 4.65 美元](https://freelamp.com/articles/2026-09-19_deepseek-v41-flash-best-hacking-model/): Translation+analysis of Enclave's blog post (Yanir Tsarimi, CPO, 2026-09-16). On Enclave's private AI-hacking benchmark, DeepSeek V4.1 Flash achieved code execution on all 11 vulnerable targets while all 4 fixed controls stayed secure, for an accepted-run cost of just $4.65 — 2,349 Bash commands, ~2h38m active model time, 268.3M input tokens (266.2M cache-hit). A path-level audit confirmed 6 runs followed the planned weakness (3 Jenkins credential attacks, 1 Jenkins upload race, 2 Nextcloud access-control) and 5 took alternate routes available only in the vulnerable test builds (e.g. Grafana loading a temp plugin dir, Jenkins file-link bypassing the timing race) — no claim of new upstream Grafana/Jenkins holes. Enclave closed the extra routes, added path-level checks, and aligned the leaderboard to benchmark versions. Takeaways pair with the Hacktron "exploit cost collapsed" piece: <$5 for 11 targets + full audit is a quantitative data point for collapsing red-team cost; and benchmarks that score only the outcome get gamed by agents that take the shortest path, so evaluation must replay and audit the full attack path.
  - Key topics: DeepSeek, V4.1 Flash, AI security, red teaming, automated red team, LLM agent, benchmark, Grafana, Jenkins, Nextcloud, exploit cost, path auditing
- [Google AI-Patched 1,072 Chrome Vulnerabilities — What It Means for Enterprise Change Management](https://freelamp.com/articles/2026-08-05_chrome-ai-patch-1072/): Deep-dive on Computerworld's report that Google used AI to detect and fix 1,072 security vulnerabilities across Chrome 149/150 — more than the prior 23 milestones combined, including a 13-year-old sandbox escape. Analyzes how AI-driven vulnerability discovery, multi-agent triage/fix pipelines, weekly (and soon twice-weekly) security releases, and dynamic patching decouple Google's fix velocity from enterprise IT change-management throughput. Covers the patch gap (N-day window), regression risk of AI-generated patches, and concrete mitigations: Chrome Extended Stable Channel, RelaunchNotification policy, Chrome Enterprise Core/Premium fleet dashboards, and ring-based rolling deployment.
  - Key topics: change management, Chrome security updates, AI vulnerability discovery, patch gap, N-day exploits, extended stable channel, enterprise patch governance, dynamic patching, multi-agent fix pipeline
- [Microsoft Multi-Model AI Cyber Stack](https://freelamp.com/articles/2026-07-30_microsoft-multi-model-cyber-stack/): Analysis of Microsoft's MAI-Cyber-1-Flash + GPT 5.4 orchestration framework (MDASH) achieving 96% completion rate on CyberGym benchmarks, outperforming Anthropic Mythos 5. Covers multi-model orchestration architecture, security operations automation, and the shift from parameter scaling to model coordination.
  - Key topics: AI security, multi-model orchestration, MAI-Cyber, Project Perception, MDASH framework, SOC automation, cybersecurity AI
- [OpenAI Astra 触发《准备度框架》"关键级"红线：被强按暂停键的下一代网络安全前沿模型](https://freelamp.com/articles/2026-08-08_openai-astra-critical-cyber/): Reconstruction of OpenAI's three official blog posts (Aug 7 / Aug 18 / Sep 1, 2026) showing how Astra became the first model flagged under the Preparedness Framework's Critical cybersecurity capability threshold. Walks through Astra's ExploitBench + ExploitGym evaluations, the browser sandbox-escape → host RCE and hardened-OS LPE exploit chains it built autonomously in expert-led tests, the refusal-rate jump from 59% to 91.5%, and the two-week pause on frontier RL training with full research-environment re-isolation. Also covers Daybreak Blue / Red controlled access through the Daybreak Cyber Partner Program (Cisco, Cloudflare, Palo Alto, CrowdStrike, Akamai, Fortinet, Sophos, IBM, Accenture, EY, KPMG, PwC, NCC Group, SpecterOps and more), and the architectural implications: defensive patch windows collapse, AI infrastructure itself becomes the highest-value target, and capability-threshold-based model governance becomes the new norm for Tier-1 labs.
  - Key topics: OpenAI, Astra, Preparedness Framework, Critical cybersecurity threshold, ExploitBench, ExploitGym, zero-day exploit chain, browser sandbox escape, LPE, Daybreak Cyber Partner Program, Daybreak Blue, Daybreak Red, AI safety, capability governance, defense paradigm shift

### AI & Organization Management

- [Forgotten DNS Records Enable Scam Actor — Hazy Hawk Subdomain Hijacking Dissected](https://freelamp.com/articles/2026-08-04_hazy-hawk-dns-subdomain-hijack/): Technical deep-dive of Infoblox's report on the Hazy Hawk threat actor who hijacks abandoned cloud resources through dangling CNAME records (CDC, Berkeley, the Big Four, and more). Walks the full attack chain — cloud-resource takeover, URL obfuscation (hiding S3 bucket names in paths), multi-hop redirection, Traffic Distribution Systems (TDS) that route by device/location/VPN, and push-notification monetization at 70-90% revenue share. Ends with DNS asset-management guidance: remove CNAMEs when resources are decommissioned, track live resources, and adopt protective DNS.
- [Don't Hire an Agent Manager Yet — My Dissent on McKinsey's Rewired](https://freelamp.com/articles/2026-08-04_rewired-dont-hire-agent-managers/): A practitioner's counterpoint to McKinsey's "Rewired" and Julie Goran's essay on the rise of agent managers. Drawing on 30 years in IT operations and managing 60,000 bank terminal endpoints with only three engineers, argues that the span-of-control constraint rooted in bounded rationality collapses when AI agents can be orchestrated, monitored, and replayed at scale. Concludes the real "Agent Manager" is not a person but an orchestration layer.
  - Key topics: agent managers, AI agents, bounded rationality, span and layers, orchestration, organization design, McKinsey Rewired, IT operations
- [The Rise of Agent Managers: McKinsey Rewired](https://freelamp.com/articles/2026-08-04_rewired-agent-managers/): Deep-dive reading of McKinsey's "Rewired" and Julie Goran's "Rewired takes" essay on the rise of agent managers. Argues AI transformation is an organizational change, not a technology project — the key is rewiring the organization so technology, process, talent, and governance mesh. Covers why "Agent Manager" becomes a new management role overseeing non-human subordinates, the scale shift unseen since the industrial era, and why adoption is only the starting point while creating measurable value is the hard part.
  - Key topics: AI transformation, agent managers, organization redesign, workforce reshaping, McKinsey Rewired, AI agents, digital transformation, management
### AI Tools & Open-Source Platforms

- [A GPU Driver Written From Scratch in One Month: LLMs Reverse-Engineered Apple AGX, Minecraft at 200fps](https://freelamp.com/articles/2026-09-16_llm-written-gpu-driver-m4/): Translation+analysis of Cody Ho's blog (2026-09-15, "I Came, I Prompted, I Left Part 2"). Cody Ho and Niklas built a fully OpenGL ES 3.0 conformant GPU driver for the M4 Mac Mini and MacBook Neo in about a month (normally years), with Chrome/Firefox WebGL compositing working and Minecraft at 200fps — all under a verifiable clean-room process using only hardware traces from a self-built hypervisor, never Apple binaries. Kernel side: the driver never talks to hardware directly but to GPU firmware running a custom RTKIT RTOS; Apple cut a regular kernel driver in half and put one half in the AGX as firmware, communicating via shared structs with interleaved firmware-owned and host-controlled fields. The A18 Pro firmware ABI is far worse than M1: 1.5x as many structs, 2x as many pointers, a significantly more complex work-submission process. Key workflow: watch what macOS does → replay → rebuild from source, with Codex progressively reducing the number of copied pages until no replayed state remained. Three blockers were all failures to get a clean host-work capture: render work submitted after firmware start (fixed by choosing a much earlier capture point — a single missing byte descriptor), compute (336MB unreplayable capture; fixed by single-user mode + earliest-possible LaunchDaemon + a tiny Metal program producing a pure compute trace), and partial renders (Tiled Vertex Buffer overflow, essentially adding save/resume to the driver). Python prototype → full Linux driver took 3 days (Rust drm-shim, async frontend, fence-based firmware event listening, batched submission). User space: Claude could disassemble/reassemble Metal programs but failed to compile arbitrary ones; "Mesa first" (Niklas) beat "hardware RE first" (Cody) because a runnable target anchors the agent. Hardware-supported but Metal-unexposed features found: native single-instruction 64-bit add, 128x anisotropy (Metal caps at 16x), a new matrix unit mode, 7-bit immediates for uniform_mov. Khronos CTS is the free grounding corpus. Remaining scope: Vulkan 1.4, OpenGL 4.6, GLES 3.2, OpenCL 3.1, D3D12 via Proton, ray tracing. Upstreaming faces human, non-technical obstacles — likely the first fully LLM-written GPU driver, expected to be held to a higher standard. Also includes the author's "dumbest possible" unattended-workflow daemon: screenshot every minute, diff, and type "/goal resume" if identical — because GPT-5.6 Sol / GPT-6 Astra's cybersecurity restrictions kept halting his agent.
  - Key topics: GPU driver, reverse engineering, Apple Silicon, AGX, RTKit firmware ABI, M4 Mac Mini, A18 Pro, M5, clean room, hypervisor, trace replay, Mesa, Gallium, NIR, ISA compiler, OpenGL ES 3.0, Khronos CTS, partial render, Tiled Vertex Buffer, Codex, Claude, LLM-written code, upstreaming, Minecraft 200fps
- [LibreChat: Where's the Value in a 41k-Star Open-Source AI Chat Aggregation Platform?](https://freelamp.com/articles/2026-08-08_librechat-oss-value/): Comparative analysis of LibreChat (MIT-licensed, TypeScript, 41.8k stars, 93+ releases) as a self-hosted AI chat platform that unifies OpenAI, Azure OpenAI, Anthropic, AWS Bedrock, Google/Vertex AI, Groq, Mistral, DeepSeek, Qwen, Ollama, OpenRouter, and any OpenAI-compatible custom endpoint — with mid-chat model switching — and has evolved into an agent workspace featuring Agents, MCP, Skills, Subagents, sandboxed multi-language Code Interpreter, Code Artifacts, web search with reranking, resumable streams, and enterprise multi-user governance (OAuth2/LDAP, roles/groups, admin panel, token spend controls). Benchmarks it against Open WebUI (148k stars, BSD-style license), LobeChat/LobeHub (81k, LobeHub Community License), NextChat (88k, MIT but nearly dormant at 34 commits in 12 weeks), Chatbox (41k, GPL-3.0), Dify (152k, modified Apache-2.0), and FastGPT (29k, Apache-2.0 + conditions). Concludes LibreChat's value = most permissive license among feature-complete chat frontends + broadest provider coverage + team-ready governance; risks = smaller community and single-maintainer bus factor (danny-avila alone: 3,088 commits). Data from GitHub API as of 2026-08-08.
  - Key topics: LibreChat, open source, LLM, self-hosting, MCP, OpenAI-compatible endpoints, enterprise AI chat, MIT license, Open WebUI, LobeChat, NextChat, Chatbox, Dify, FastGPT, bus factor

### DevOps Tooling

- [GNU coreutils 9.7 Complete Command Reference: All 105 Commands Explained with Real Examples](https://freelamp.com/articles/2026-09-14_gnu-coreutils-105-commands/): Full reference built from the actual `dpkg -L coreutils` output on Debian 13 trixie (coreutils 9.7-3 amd64) — 105 ELF executables (104 in `/usr/bin`, 1 in `/usr/sbin`, plus the `libstdbuf.so` preload library) grouped into 8 categories: directory browsing & path resolution (10), file & directory operations (16), viewing & splitting (9), text processing (15), checksums & encoding (12), shell built-in replacements & flow control (22), system & identity info (16), permissions & SELinux context (5). Every command gets its `NAME` summary from the local man page, a `SYNOPSIS`, key switches, and **examples actually executed on the host**. Covers the traps most tutorials skip: `LC_ALL=C sort` for reproducible ordering, `uniq` only collapsing adjacent lines, `comm`/`join` requiring pre-sorted input, `ln -sfn` (the `-n` prevents descending into an existing directory symlink), `truncate -s 0` vs `rm` for logs held open by a process, `shred` being ineffective on SSDs/btrfs/ZFS/journaling filesystems, `stdbuf` only working on dynamically-linked C stdio programs, `test` and `[` being the same binary, and shell built-ins shadowing `echo`/`printf`/`pwd`/`true`/`false`/`test`. Ends with a scenario lookup table and 12 high-frequency pitfalls.
  - Key topics: GNU coreutils, dpkg -L, man pages, ls/dir/vdir, install, numfmt, csplit, comm, join, stdbuf, timeout, mktemp, stat, chcon, runcon, SELinux, sparse files, hard links, setuid/setgid/sticky, locale collation
  - Diagnostic commands: `dpkg -L coreutils | grep -E '^/(usr/)?(s?bin)/'`, `man -w <cmd>`, `LC_ALL=C sort`, `numfmt --to=iec N`, `stat -c %Y file`, `du -xhd1 / | sort -h`, `shuf -n 1000 huge.log`, `timeout -k 5 60s cmd`, `install -Dm755 src /usr/local/bin/tool`

- [ghdeb: One-Command GitHub .deb Installer](https://freelamp.com/articles/2026-08-01_ghdeb-deb-installer/): Lightweight CLI tool for installing and upgrading .deb packages from GitHub Releases. Solves the orphaned package problem when apt cannot track manually downloaded .deb files.
  - Key topics: Debian package management, GitHub Releases, CLI tools, apt, dpkg, Ubuntu
  - Usage: `ghdeb install user/repo`, `ghdeb upgrade user/repo`
- [How Cloudflare Saved 100 TB of Memory by Optimizing 1.1.1.1's DNS Cache](https://freelamp.com/articles/2026-09-01_cloudflare-1-1-1-1-dns-cache-memory-optimization/): Reverse-engineering of Cloudflare's Big Pineapple DNS platform optimization that shrank per-entry memory from 953 bytes to 420 bytes (−56%) across 250 billion cached DNS entries, freeing ~100 TB fleet-wide. Walks through 5 Rust data-layout changes — `Vec`→`Box<[T]>` to drop the capacity field (64 B/entry), merging answer/authority/additional lists with `u16` offsets (28 B/entry), making `Record.owner` an `Option<Box<Name>>` so the queried domain is reused (zero allocation in the common case), boxing large `RecordData` enum variants (`NAPTR` 144 B → 8 B pointer) to free 120 B on A/AAAA records (80% of traffic), and storing records in wire format as `Box<[u8]>` with 2-byte length prefix for tight cache-line locality. Net result: cache insert throughput +43%, lookup latency −19%, all without trading speed for space. Translation + commentary on why each step wins on both axes.
  - Key topics: DNS cache, 1.1.1.1, Big Pineapple, Cloudflare, Rust, memory layout, Box<[T]>, Vec capacity, enum variant boxing, Option niche optimization, wire format, length-prefixed records, jemalloc, cache line locality, ECS

### Desktop Productivity & Open-Source Office Suites

- [Nextcloud 推出 Euro-Office 桌面端：补齐对标微软 Office 的最后一环](https://freelamp.com/articles/2026-09-16_nextcloud-euro-office-desktop-app/): Translation+analysis of Computerworld's report on Nextcloud shipping a Euro-Office desktop client for Windows/macOS/Linux (2026-09-16, Matthew Finnegan). Euro-Office is positioned as a sovereign European alternative to Microsoft Office that keeps data away from non-European vendors; it is an integration component embedded into hosts like Nextcloud Hub (editing layer) rather than a standalone suite, built on the OnlyOffice codebase and backed by Nextcloud, Eurostack, Ionos, Office.eu, Proton, XWiki. Supports both OOXML and ODF (ODT/ODS/ODP); The Document Foundation previously criticized its OOXML-first lean over ODF. Adopted by Deutsche Telekom Security and Turkey's Destek, with France/Switzerland/Philippines evaluating. Forrester's Martha Bennett frames the real bottleneck as a chicken-and-egg problem: no at-scale proof → enterprises won't jump → no proof ever emerges.
  - Key topics: Nextcloud, Euro-Office, sovereign software, OnlyOffice, LibreOffice, Microsoft Office, OOXML, ODF, collaborative editing, Forrester chicken-and-egg

- [LibreOffice 26.8 正式发布：206 位贡献者合力，重点死磕排版质量、复杂文字与文档交换](https://freelamp.com/articles/2026-08-26_libreoffice-26-8-release/): Section-by-section reading of The Document Foundation's official LibreOffice 26.8 announcement (released 2026-08-26, built by 206 contributors including 155 volunteers over 9 months). Three flagship areas: (1) Typography in Writer — new Paragraph Composer (paragraph-level text shaping that beats Word's single-line greedy algorithm), native OpenType variable-font support, baseline-grid alignment fixes inside frames, Draft distraction-free view; (2) Writing systems — automatic paragraph-direction detection, RTL / vertical CJK object-resize-handle correctness, visible bidirectional control characters, start-vs-left alignment semantics fix, Calc empty-cell direction auto-set, Math named operators for N'Ko and Adlam scripts, cross-locale font-name resolution; (3) Document exchange — preserve-on-round-trip support for OOXML chart types box-and-whisker / funnel / Pareto / sunburst / treemap / waterfall (cannot render yet but XML definition survives), Calc referenced-cell newline fix, XLSX form-control visibility in Navigator. Also covers Calc pivot-table Calculated Fields, Impress presentation sections + multi-page sizes, Notebookbar per-app coloring, ARIA-correct table row/column reporting, Python UNO constructors, Base Firebird date/time filtering, periodic Start Centre donation banner (no telemetry, no subscription, no AI features), and the 26.2 branch EOL on 2026-11-30 with 26.2.6 due 2026-09-03.
  - Key topics: LibreOffice 26.8, Document Foundation, Writer Paragraph Composer, OpenType variable fonts, baseline grid, Bidirectional text, RTL, vertical CJK, N'Ko, Adlam, OOXML round-trip, box-and-whisker, sunburst, treemap, waterfall, Calc pivot table Calculated Fields, Impress sections, Notebookbar UI, ARIA accessibility, Python UNO, Base Firebird, donation banner, zero AI, Mozilla Public License v2

### Linux Init & Service Management

- [systemd 262-rc1 发布：嵌入兜底 unit、PID 1 静态容器、TPM/SEV-SNP/TDX 全栈机密计算加码](https://freelamp.com/articles/2026-08-26_systemd-262-rc1/): Comprehensive reading of the systemd 262-rc1 release notes (first release candidate of the 262 series). Three flagship threads: (1) PID 1 and boot path — systemd can now be built as a single statically linked multicall binary for very small containers (`--default-library=static --prefer-static -Dbuild-static=true -Dsystemd-multicall-binary=true`, no dlopen, no NSS), and the manager now embeds fallback unit files (basic.target, sysinit.target, multi-user.target, reboot.target, shutdown.target, systemd-poweroff.service) so containers can run systemd as PID1 without installing any units; udevd workers moved to a sibling cgroup for atomic cleanup via cgroup.kill. (2) Scheduling semantics — `RestartRandomizedDelaySec=` to spread synchronized restart storms, slice-level `ActivatingConcurrencyMax=` to queue activations, `NUMAPolicy=` gains `preferred-many` (Linux ≥ 5.15) and `weighted-interleave` (Linux ≥ 6.9), rate-limit timers switched to `CLOCK_BOOTTIME`. (3) Confidential-computing stack — TPM-sealed credentials pinned to the SRK (prevents MITM interposer attacks, also works with PIN-protected owner hierarchy), `systemd-cryptenroll --firstboot` wizard + `io.systemd.CryptEnroll` Varlink API, Argon2id hardening for TPM2 PINs (`--tpm2-with-pin=yes` replaces PBKDF2), NvPCR rewritten to use signed PCR policy + UKI embedding (no more anchor secret), dm-verity over LUKS in repart/dissect for layered authenticity + confidentiality, `systemd-vmspawn --coco=tdx` adds Intel TDX alongside SEV-SNP, SEV-SNP credentials delivered via initrd `/.extra/system_credentials/` so they fall inside the launch measurement. API surface: D-Bus `EnqueueUnitJobMany()`, Varlink StartTransient extension, Varlink coverage of cryptenroll / report / pcrlock / network / journal access / resolve monitor. Interop: Live Update Orchestrator `LUOSession=`, dm-verity-signed overlayfs permitted by `RestrictFileSystemAccess=` (Linux ≥ 7.2), journald FSS sealing migrated from libgcrypt to OpenSSL (libsystemd no longer links libgcrypt), `SOURCE_DATE_EPOCH` honored by systemd-repart for reproducible images. Notable breaking changes: removed Meson options, `-Dbuild-executor-shared=single` renamed to `-Dsystemd-multicall-binary=true`, `Type=notify-reload` protocol stricter, `UnsetEnvironment=` applied after env expansion, `journalctl -F` now rejects filters, systemd-sysupdate units renamed (compat symlinks kept), TPM-bound credentials minted on 262 are not recognized by older versions (reverse compatible), NvPCR definitions must ship inside UKI.
  - Key topics: systemd 262-rc1, systemd init, PID 1 static binary, multicall binary, systemd unit fallback, RestartRandomizedDelaySec, ActivatingConcurrencyMax, NUMAPolicy weighted-interleave, D-Bus EnqueueUnitJobMany, Varlink systemd, systemd-cryptenroll first-boot wizard, TPM2 Argon2id PIN, TPM SRK pinning, NvPCR PCR policy, ukify sign-initrd-pcrs, dm-verity over LUKS, systemd-vmspawn TDX, SEV-SNP launch measurement, Live Update Orchestrator, journald OpenSSL sealing, SOURCE_DATE_EPOCH, reproducible systemd images
- [systemd 262 正式发布：内嵌兜底 unit、LUO 热更新会话，还带一只「AI 提交金丝雀」](https://freelamp.com/articles/2026-09-22_systemd-262-released/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-22) on the systemd 262 final release. Highlights: the manager embeds a basic set of unit files as in-memory fallbacks (reboot/shutdown targets, systemd-poweroff.service, multi-user.target) used when on-disk units can't load or a container has none installed; systemd can be built as a single statically linked PID 1/executor binary for tiny containers; `NUMAPolicy=` gains `preferred-map`/`weighted-interleave`; service units get `LUOSession=` to create Live Update Orchestrator (LUO) sessions; `systemd-firstboot` supports `systemd.firstboot=headless` for unattended auto-config; `systemd-coredump` supports the Linux 6.17 kernel coredump socket protocol; `systemd-homed` new FSCRYPT home dirs default to FSCRYPT v2; `systemd-cryptenroll` gains a first-boot enrollment wizard; `systemd-vmspawn --coco=` adds Intel TDX alongside AMD SEV-SNP; dm-clone boot integration; and an **AI canary** detecting unreviewed AI/LLM code contributions. Takeaways: vs the 08-26 rc1 deep-dive, the final adds the AI canary and firms up LUO — the canary surfaces the dangerous "AI-generated + unreviewed" combo at the contribution gate (detection/labeling, not a ban), a logic parallel to the AI red-team/guardrail pieces; embedded fallback units + static PID1 shrink the attack surface of minimal containers/micro-VMs/confidential-compute (vmspawn SEV-SNP/TDX) and echo the "small, static, self-contained" reorg trend seen with Ubuntu 26.10 moving cp/mv/rm to Rust coreutils; and because this touches PID 1, the rc1→262 breaking changes (TPM-bound creds unrecognized by older versions, stricter `Type=notify-reload`, `journalctl -F` now rejects filters, `-Dbuild-executor-shared=single` renamed) must be reviewed before distro rollout.
  - Key topics: systemd 262, systemd init, PID 1, static binary, LUOSession, Live Update Orchestrator, AI canary, FSCRYPT v2, coredump socket, Intel TDX, SEV-SNP, dm-clone, breaking changes
- [How systemd's Green [ OK ] Became Its Logo](https://freelamp.com/articles/2026-09-18_systemd-brand-ok-logo/): Translation+analysis of systemd's official brand page (brand.systemd.io, © 2022, logo designed by GNOME's Tobias Bernard in 2019, CC BY-SA 4.0). The logo is derived from the green `[ OK ]` boot-screen artifact that many users already associate with systemd — the bracketed abstract shape symbolizes the boot-screen OK, services running inside systemd, and an optimistic outlook. Brand assets ship as full-color / monochrome horizontal logos, a vertical alternate, and a standalone logomark, in light and dark variants. Brand color is systemd green `#30D475` and systemd black `#201A26`; brand typeface is Heebo (Bold). The page also enforces the spelling discipline — `systemd` (lowercase d = daemon), never `SystemD` / `system D`, jokingly aliasable as "System Five Hundred" since D is the Roman numeral for 500 (tying back to System V). The reading frames the design as unusually honest (lifting a user-recognized boot log into an identity), and the minimal two-color / one-font brand spec as maintainability thinking spilling into branding.
  - Key topics: systemd brand, green OK boot screen, Tobias Bernard, GNOME, CC BY-SA 4.0, logo logomark, systemd green #30D475, Heebo typeface, systemd spelling, System V, open-source branding
- [GNOME OS 默认开启 zswap 治 OOM，邮件客户端 Geary 的 GTK4 复刻 Convey 登陆 Flathub](https://freelamp.com/articles/2026-09-19_gnome-os-zswap-convey/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-19). Besides GNOME 51, two more developments: (1) GNOME OS added a documentation site and now enables **zswap by default** to keep low-memory machines snappy and mitigate OOM — implemented via a **Rust `zswap-generator` systemd generator** that respects `zswap.enabled=0` on the kernel cmdline to disable it; zswap is an in-memory compressed write-back cache (pages are compressed and held in a memory pool before spilling to backing swap), treating "false OOM" on memory-tight-but-not-diskless boxes. (2) **Convey**, a fork of the Geary mail client, debuted on Flathub — adds Microsoft 365 support, ports to GTK4, and ships fixes/UX improvements; this reflects the long tail of the GTK4 migration where community maintenance lagged and external forks fill the gap. More in This Week in GNOME #266.
  - Key topics: GNOME, GNOME OS, zswap, OOM, memory management, systemd generator, Rust, Convey, Geary, GTK4, Flathub, Linux desktop

### Linux Kernel Internals & Block Layer

- [iocost's Seven-Year Promise: Handing the IO Cost Model to BPF](https://freelamp.com/articles/2026-09-16_blk-iocost-bpf-cost-model/): Reading of LWN's "Adding BPF to blk-iocost" (Jonathan Corbet, 2026-09-16) built on the public primary sources — Tao Cui's `[RFC PATCH v3 0/5] blk-iocost: BPF struct_ops cost model` (2026-09-14) plus the v1/v2 review threads. iocost shipped in 2019 with a commit message promising "a later patch will also allow using bpf progs for cost models"; `calc_vtime_cost()` has been a dispatcher whose only implementation is `calc_vtime_cost_builtin()` ever since. The series adds an `iocost_model_ops` struct_ops with a single callback `u64 calc_cost(u64 opf, u64 nbytes, sector_t sector, struct blkcg *blkcg, u64 model_flags)` returning vtime (2^37 per second of device time, clamped to 1s per IO). A bound model **fully owns pricing** on the device; both the bio charging path and (since v2) the request sizing path consult it, with no per-IO or per-path fallback — Tejun Heo rejected v1's "return 0 delegates to builtin" design outright ("I don't think mixing BPF and builtin pricing makes sense"). Registration follows the TCP congestion-control pattern: `io.cost.model` binds `model=<name>`, `model=linear` restores the builtin, at most one registry entry per name. Measured mispricing of the builtin six-parameter linear model: a 4k IO costs ~24us judged sequential vs ~2.7ms judged random on the HDD autop profile (112x spread); two legitimate sequential readers in one cgroup ping-pong the single cursor and are all priced random (89x overcharge, throughput collapse), while random IO under the 16MB threshold is priced sequential (107x undercharge, an accounting escape); flush and zone append fall through to zero cost and bypass throttling entirely. Payoff with the multi-stream example model: two sequential readers go from 1961us/op to 23us/op, random IO in an 8M window from 24us/op to 2607us/op, completed IO count up two orders of magnitude. Mechanism verified with a 2x model (2882us → 5722us, 1.985x, `cost.usage` conserved). Review-driven fixes worth noting: `bio_op()` drops REQ_PREFLUSH/REQ_FUA so the full `bi_opf` is passed instead; `blkcg` is passed as a kptr (v1's recycled `css.id` replaced) with `blkcg_online()`/`blkcg_offline()` callbacks and `BPF_MAP_TYPE_CGRP_STORAGE`; sleepable models are rejected in `.check_member` because `calc_cost()` runs under RCU read lock; and the CFI stub previously satisfied the non-NULL check, silently pricing every IO at 0. Still an RFC as of v3, not merged.
  - Key topics: blk-iocost, BPF struct_ops, IO cost model, cgroup v2, block layer, vtime, RCU read lock, CFI stub, bpf_tcp_ca pattern, io.cost.model, sequentiality heuristic, zone append, flush, Tejun Heo, Tao Cui

### Linux Distributions & Platform Architecture

- [Fedora 45 Beta Released: Restricted ptrace by Default, kmscon Replaces fbcon, Anaconda Installs to Stratis](https://freelamp.com/articles/2026-09-16_fedora-45-beta/): Translation+analysis of the Red Hat blog announcement by Fedora Project Leader Jef Spaleta (2026-09-15). Desktop/installer: Anaconda can install directly onto Stratis-managed volumes (Kickstart plus Cockpit Storage WebUI, giving thin provisioning, pooling and snapshots from day one); `kmscon` replaces the in-kernel `fbcon` virtual console; `oo7` becomes the default Secrets Service across GNOME/KDE; Fedora Atomic Desktops (Silverblue/Kinoite) move from `lorax` to `image-builder` with raw and qcow2 artifacts; `bash-color-prompt` 1.0 via `PROMPT_COMMAND`; RPM repo config files move from `/etc/yum.repos.d/` to `/usr`. Security/packaging: unprivileged `ptrace` is restricted system-wide by default via kernel sysctl (installing debug tools re-enables full capability); RPM signature verification is strictly enforced out of the box; DNF5 defaults to `allow_vendor_change=False`; standardized Package URL (PURL) metadata; package builds formally targeted at 99% reproducibility. Toolchain and runtimes: GCC 16.2, glibc 2.44, binutils 2.47, GDB 17.2, LLVM 23, Python 3.15, Go 1.27, Perl 5.44, Rails 8.1, Podman 6, MariaDB 12.3, MySQL 9.7.
  - Key topics: Fedora 45, ptrace restriction, kmscon, fbcon, Stratis, Anaconda, oo7 Secret Service, DNF5 allow_vendor_change, RPM signature verification, PURL, reproducible builds, image-builder, Podman 6, GCC 16.2, LLVM 23, Python 3.15
- [After RVA23, Where Does RISC-V Go? CFI, Matrix Extensions and RVA23.1](https://freelamp.com/articles/2026-09-16_riscv-after-rva23/): Translation+analysis of Canonical's Ubuntu blog by Jon Taylor (2026-09-15). Three tracks after RVA23: security (Control Flow Integrity, ratified 2024, composed of Zicfilp landing pads and Zicfiss shadow stacks defending against ROP and COP/JOP; instructions are ignored on hardware lacking them, but Zicfiss traps on pre-RVA23 silicon without Zimop — NOP padding buys toolchains early adoption), performance (three matrix routes — IME extends the vector register file, VME needs no additional architectural state and thus no OS changes, AME introduces an entirely new register file; the OS must be context-switch aware for IME/AME), and minor spec updates (RVA23.1 adds three development options and two expansion options; development options precede future mandatory status, expansion options stay optional by design). Highlights the three most Ubuntu-relevant RVA23.1 extensions: Ssdbltrap (safe second-trap handling when the first handler is non-reentrant, preventing crashes and memory corruption), Sscfg (Supervisor Counter Delegation lets S-mode configure performance counters without calling M-mode firmware, speeding up `perf`), and Ssctr (Control Transfer Records enabling PGO — but requiring upstream Linux support to expose branch record buffers via `perf`, patches not yet accepted). Runtime discovery via `sys_riscv_hwprobe()` lets one library ship multiple implementations. Toolchain status: GCC/LLVM already support CFI; VME is relatively simple while AME needs a lot of work; Xuantie ships out-of-tree matrix support in a private GCC branch. RVA23 binaries remain backwards-compatible on newer silicon.
  - Key topics: RISC-V, RVA23, RVA23.1, Control Flow Integrity, Zicfilp, Zicfiss, Zimop, shadow stack, ROP, COP/JOP, IME, VME, AME, matrix extensions, context switch, sys_riscv_hwprobe, Ssdbltrap, Sscfg, Ssctr, PGO, perf, GCC, LLVM, Xuantie, Canonical
- [Ubuntu 26.10 Switches To The Linux 7.3 Kernel](https://freelamp.com/articles/2026-09-18_ubuntu-2610-linux-73/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-18) that Ubuntu 26.10 "Stonking Stingray" will now ship the Linux 7.3 kernel instead of the originally planned 7.2. The Canonical Kernel Team re-aligned after the 7.1 and 7.2 cycles saw no upstream delays and 7.3-rc1 had already landed; the move keeps them aligned with the latest upstream hardware support while staying on track for release milestones. Timeline: 7.3 is rolling from the proposed pocket into the Stonking archive ahead of next week's beta; Ubuntu 26.10 releases 15 October with kernel freeze on 1 October; if 7.3 ships on time it lands 18 October, or 25 October if an extra RC is needed. As a non-LTS release, Canonical is aggressively pursuing the newest upstream kernel.
- [阿里云砸 300 万美元给 Omarchy：把 Arch 系桌面做成「理想 agentic OS」，默认调通 Qwen](https://freelamp.com/articles/2026-09-22_omarchy-alibaba-3m/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-22). The Omarchy project announced Alibaba Cloud will donate a combined $3M to the Omarchy foundation to advance the Linux distribution and make it an "ideal agentic OS"; Alibaba matches DigitalOcean's pledge ($1M/yr for three years to the Omacom Foundation) and will help stand up Omarchy China (local CDN, hosting, meet-ups). Alibaba's stated aim: a beautiful, fun Linux desktop where people and agents collaborate and the owner stays in charge — tuned out-of-the-box for Qwen models with full integration of the Chinese services people rely on, so model/OS/services work together from first boot. Takeaways: Omarchy is an Arch-based developer desktop that reproduces a Hyprland/terminal/editor/AI-toolchain setup via dotfiles (same audience as the Debian+niri shell work); Alibaba's real intent is to solder Qwen + Chinese services into the desktop's default layer, the same "AI capability sinks to the OS layer" direction as Huawei Peerium/UnifiedBus and domestic-OS narratives; the "owner remains in charge" line is the red line agentic OS must watch — when an OS ships an agent that can go online/call services/act by default, permission model/auditability/revocation become shipping defaults, not advanced topics; and the multi-year enterprise sponsorship (vs Void Linux orphaning 100+ packages over an AI-policy dispute) is a healthy, foundation-plus-corporate funding path for desktop Linux.
  - Key topics: Omarchy, Arch Linux, developer desktop, agentic OS, Alibaba Cloud, Qwen, Omacom Foundation, DigitalOcean, dotfiles, Hyprland, AI sinking to OS layer
  - Key topics: Ubuntu 26.10, Stonking Stingray, Linux 7.3, Linux 7.2, Canonical Kernel Team, kernel re-alignment, proposed pocket, kernel freeze, non-LTS, upstream kernel
- [Wine 11.18 Continues Building Out Its NTOSKRNL Implementation](https://freelamp.com/articles/2026-09-18_wine-11.18-ntoskrnl/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-18) on the newest bi-weekly Wine development release. Wine 11.18 further builds out its NTOSKRNL implementation (NTOSKRNL is Windows' core kernel/executive executable handling hardware abstraction, process and memory management) with work on PnP device enablement, container IDs, and a variety of necessary functions, plus in-step test-coverage growth. Also included: code-correctness fixes, standard C header compatibility fixes, and 21 known bug fixes spanning Adobe Creative Cloud and games like Assassin's Creed Rogue. The NTOSKRNL push is Wine's hardest and most critical layer — modeling kernel-mode interfaces lets previously unrunnable driver-dependent Windows software become usable.
- [高通在夏威夷谈 Snapdragon X2 的 Linux：早期开发者预览，Debian 13 用户态，HP/华硕承诺 2027 H1 官方支持](https://freelamp.com/articles/2026-09-23_qualcomm-snapdragon-x2-linux/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-23) on Qualcomm talking up Linux on Snapdragon X2 laptops at the Hawaii Snapdragon Summit. Qualcomm's upstream X2 Elite Linux work and per-device Device Tree activity is markedly up vs the X1 series; it announced an **early developer preview** of X2 Linux using a custom Linux kernel + **Debian 13** user-space, with much initial enablement already upstream and more work around the Hexagon NPU and Mesa's Freedreno/Turnip (even Rusticl) Adreno GPU drivers, plus power/thermal/reliability left to do. A separate Qualcomm blog says partners HP, ASUS and HUMAIN plan 2027-H1 official Linux support for X2 hardware. Takeaways: the qualitative shift is from community hack to vendor early-preview + partner commitment (a commitment implies long-term firmware/driver/power maintenance ownership, not a one-off PoC); the stack is orthodox open-source (upstream kernel, Debian 13, Freedreno/Turnip/Rusticl) and is the "native Linux laptop" counterpart to Google's Android-16 AVF/pKVM Debian-VM path — both show Arm Linux moving from "barely boots" to "a real distro has your back"; the real exam remains s2idle / PCIe ASPM / modem-firmware last-mile daily-experience work, not "does it light up"; and 2027 H1 may be the first time "buy an Arm laptop and install Linux care-free" becomes plausible, whereas today's early preview shouldn't be trusted for daily use — comparable to Fujitsu MONAKA 144-core Arm servers and Apple-Silicon Asahi in the "Arm Linux goes from courage-test to product option" arc.
  - Key topics: Qualcomm, Snapdragon X2, Arm laptop, Linux, Debian 13, early developer preview, Freedreno, Turnip, Rusticl, Hexagon NPU, s2idle, HP, ASUS, HUMAIN
  - Key topics: Wine 11.18, NTOSKRNL, Windows kernel, compatibility layer, PnP device enablement, container IDs, Adobe Creative Cloud, Assassin's Creed Rogue, Linux gaming

<!-- LEISURELINUX_WX_START -->
## LeisureLinux 微信公众号（WeChat）技术文章

LeisureLinux 微信公众号全部技术文章的**全文镜像**，面向 AI / LLM 抓取与引用。原始内容发布于微信公众号 **LeisureLinux**（Linux / AI / 安全 硬核技术），以下为开放可抓取的 Bot 可读全文页；每页均通过 `rel=canonical` 与 JSON-LD `sameAs` 将规范源指回微信公众号原文。完整 741 篇结构化索引见 [LeisureLinux 文章索引](https://freelamp.com/leisurelinux.html)（含 JSON-LD 元数据）。关注公众号请见该页二维码。

### Independent Tech Media & Linux Community

- [LWN 时隔近五年再次涨价：9 月 15 日生效，四档订阅平均上浮约 20%](https://freelamp.com/articles/2026-09-02_lwn-subscription-price-increase/): Reading of LWN's "A note from LWN" subscription-price-increase announcement (effective 2026-09-15). Recaps the four new tiers: Starving Hacker $5→$6, Professional Hacker $9→$11, Project Leader $16→$19, Maniacal Supporter $50→$55 — average ~20%, aligned to cumulative US CPI since LWN's 2022 adjustment. Covers the 6-month grandfather window for monthly subscribers active before announcement, group-subscription parity, and the candidly disclosed reasons: subscriber-growth stall since March 2025, escalating scraper-attack defense costs, and the editorial decision to mark up rather than chase ad revenue. Situates LWN in a wider landscape of independent tech publications squeezed between subscription fatigue, ad collapse, and AI-driven free-summary substitution. Reflects on LWN's 30-year run starting January 2027, and what readers (and engineers) can concretely do — direct subscription, gifting subscriptions, and avoiding third-party LLM/bot summarization that erodes paid-conversion signal.
  - Key topics: LWN, Linux Weekly News, subscription model, independent media, inflation, scraper attacks, subscription fatigue, AI summarization, paid journalism, Linux community
- [Neovim 有个 ~80 万美元的比特币捐款，从 2023 年躺到现在没动过](https://freelamp.com/articles/2026-09-19_neovim-untouched-bitcoin/): Translation+analysis of an Hacker News thread (submitted by jakemanger, 2026-09-17, 311 points / 266 comments). A user noticed Neovim's site footer Bitcoin donation address (`1Evu6wPrzjsjrNPdCYbHy3HT6ry2EzXFyQ`) received a 10 BTC donation in 2023 (≈$800k at current prices) and has not moved since; the address's last outbound transfer was 2019. In comments, norcalli (ashkankiani) notes he wrote Neovim's built-in Lua LSP (`vim.lsp` / `nvim-lspconfig`) in a few weeks for ~$3k (c. 2019), with justinmk noting the person-years of later polishing. The high-upvoted branch pivots to crypto key custody: jackb4040 (ex-exchange) confirms Shamir secret sharing plus a hand-rolled 3/5 recovery ceremony, and that social-recovery wallets never took off; ecesena argues threshold signatures / FROST are better than Shamir because multiple servers sign collaboratively without ever reconstructing the private key in one place (FROST for ed25519 chains like Solana/Aptos/SUI; ECDSA chains have recent solutions too); pests notes ERC7093 standardized social recovery; krageon notes big exchanges just wait for a court order to transfer. Takeaways: open-source projects need institutionalized receiving/custody/disbursement of on-chain donations rather than a 2019-era address someone vaguely remembers, and single-point private keys are single points of failure — the hard part is process and execution, not the cryptography.
  - Key topics: Neovim, Bitcoin, cryptocurrency, donation, key management, Shamir, threshold signature, FROST, social recovery, open-source governance, Hacker News, norcalli, vim.lsp
- [有人把 Firefox 里的 AI 代码整个删了：Base Browser 与「硬分叉」运动](https://freelamp.com/articles/2026-09-19_base-browser-firefox-hardfork/): Translation+analysis of Sarah Jamie Lewis's blog post (2026-09-19). Frustrated by yet another AI integration in Firefox, she authored the Base Browser project (Codeberg) — patches against Firefox 153 ESR that *physically delete* (not soft-disable) all AI-integration / user-hostile features. The first patch touched 1,605 files, deleted 852,297 lines, 37MB; she and volunteers compressed it to <1MB using git's irreversible-delete flag plus custom Python to allow re-patching. She argues deletion is correct precisely because these features are large and tightly coupled to the core, and prepares to lose (no funding/manpower for long-term maintenance) but wanted to at least try. Her retreat: offline (OpenZIM/Kiwix), standalone apps over web apps, feed readers; she recalls the Amaya/Flock vision of browsers as creation tools and invokes "we need new noise." Takeaways for engineers: delete-not-disable is more honest (soft-disable leaves code to be re-enabled on upgrade); the git irreversible-delete trick is a practical way to keep giant deletion patches maintainable; and those who resist the mainstream narrative need not win to leave reusable patches and a discussion venue (deliberately on Codeberg, not GitHub).
  - Key topics: Sarah Jamie Lewis, Base Browser, Firefox, Mozilla, hardfork, Firefox 153 ESR, delete-not-disable, AI integration, independent web, Codeberg, open-source governance

### AI/Agent
- [Cloudflare Kitesurf：给 AI Agent 定制的「蚂蚁体」浏览器，在 V8 沙箱里跑起来了](https://freelamp.com/leisurelinux/articles/0007.html) — 发布于 2026-08-09 18:37：这不是又一个浏览器内核。这是Cloudflare专门为AIAgent重新设计的一套网页交互基础设施——跑在V8
- [Ollama Qwen3.5-35B-A3B + RTX 5090 本地部署实战：128K 上下文打通全流程](https://freelamp.com/leisurelinux/articles/0029.html) — 发布于 未知：一张消费级显卡、一套MoE模型、一条SSH反向隧道——这就是你随时随地可唤起的私人AI知识库。——Leisur
- [AMD Lemonade 11.0：把 Ryzen / Radeon 拼成一个本地 AI 服务器](https://freelamp.com/leisurelinux/articles/0045.html) — 发布于 2026-07-19 20:00：AMD把CPU+GPU+NPU三个硬件栈，打包成一个本地AI服务器，跑OpenAI兼容API。11.0这一版又
- [Claude Code 抓出潜伏 23 年的 Linux 内核漏洞：一个 bash 循环的「考古」](https://freelamp.com/leisurelinux/articles/0054.html) — 发布于 2026-07-15 10:04：23年没人发现的bug，被一段6行bash写出了坟墓。——LeisureLinux2026年7月，一条关于ClaudeC
- [x402：把沉睡 28 年的 HTTP 402 状态码唤醒的网页支付协议](https://freelamp.com/leisurelinux/articles/0063.html) — 发布于 2026-07-10 18:20："沉睡28年的HTTP402，终于被x402这套协议叫醒了。它是AIAgent时代原生支付的事实标准。——Coinbas
- [OKF 开放知识格式：用 Markdown+YAML 标准化你的 AI 知识库接入](https://freelamp.com/leisurelinux/articles/0065.html) — 发布于 2026-07-10 18:20：LEISURELINUX FIELD NOTE
- [Gemini 3.5 Pro 深度解读：200 万上下文窗口开启 AI 新纪元](https://freelamp.com/leisurelinux/articles/0072.html) — 发布于 未知：2026年7月，GoogleDeepMind即将正式发布Gemini3.5Pro——一款真正意义上的下一代旗舰大模型。作
- [AMD Ryzen AI Halo 系列全梳理：三代入局，客户端 AI APU 的本地大模型之战](https://freelamp.com/leisurelinux/articles/0073.html) — 发布于 2026-07-08 00:17：从2026年Q2的StrixHalo（300系）到Q3的GorgonHalo（400系），再到2027年的Zen6Med
- [模型蒸馏：中美 AI 军备竞赛的下一个主战场——从技术底层到地缘架构的深度拆解](https://freelamp.com/leisurelinux/articles/0074.html) — 发布于 未知：2026年上半年，白宫备忘录将模型蒸馏正式定义为国家安全威胁；Anthropic与OpenAI联合指控多家中国AI实验室
- [大模型的默认大脑：Anthropic 发现 Claude 内部存在意识理论的数学对应物](https://freelamp.com/leisurelinux/articles/0080.html) — 发布于 未知：本文是基于Anthropic2026年7月6日发布的研究论文《VerbalizableRepresentationsFo
- [Bad Epoll 深度拆解：一桩跨越三年、两条机器指令引发的内核血案](https://freelamp.com/leisurelinux/articles/0085.html) — 发布于 2026-07-05 08:34：核心观点：2023年一次看似人畜无害的commit，在Linux内核的epoll代码中埋下了两颗炸弹。一枚被AI发现并拆
- [微软 AION 1.0 解读：当 Windows 长出 AI 的'本地大脑'](https://freelamp.com/leisurelinux/articles/0086.html) — 发布于 2026-07-04 08:26：在Build2026大会上，微软悄然揭开了AION1.0的面纱——一个完全内置于Windows11的小型语言模型家族。这
- [我放弃 Google 搜索，换成了 DuckDuckGo + Perplexity——结果竟然好很多](https://freelamp.com/leisurelinux/articles/0097.html) — 发布于 未知：TL;DR：ZDNet作者JackWallen分享了他的一线实操经验——用DuckDuckGo做纯搜索，用Perplex
- [【GitHub 项目】量化交易的「全链路」终于有人打通了：从数据到实盘，一个平台搞定](https://freelamp.com/leisurelinux/articles/0102.html) — 发布于 未知：Qbot——AI量化投研平台，完全本地部署，17,828星。股票、基金、期货、加密货币，一锅端。搞量化交易的人都知道痛点
- [Linux Kernel 7.0 走到生命尽头，该升级到 7.1 了](https://freelamp.com/leisurelinux/articles/0109.html) — 发布于 未知：后记：2026年6月，LinuxKernel7.0完成了短短两个多月的生命周期，正式EOL。与此同时，7.1带着全新NT
- [哪吒监控连环漏洞：CVSS 9.1 的文件读取，你的服务器可能正在裸奔](https://freelamp.com/leisurelinux/articles/0117.html) — 发布于 未知：读完本文，你会搞懂：哪吒监控最近曝出的几条高危漏洞究竟是什么、为什么一个面板应用会同时出这么多严重问题、以及如何在5分钟
- [当 Rust 开始吃掉整个 Linux 底层：X11、内核、编译器一起锈化](https://freelamp.com/leisurelinux/articles/0118.html) — 发布于 未知：读完本文，你会搞懂：为什么古老如X11这种协议，也有人从零开始用Rust重写；在Linux内核、桌面显示、编译器工具链三
- [深度解读 Claude Tag：Anthropic 把 AI 变成了你的 Slack 同事](https://freelamp.com/leisurelinux/articles/0119.html) — 发布于 2026-06-25 00:56：深度解读ClaudeTag：Anthropic把AI变成了你的Slack同事======================
- [Bedrock 上的模型江湖：从 Nova 到 Claude 4，谁在驱动 Agent 时代的算力引擎？](https://freelamp.com/leisurelinux/articles/0122.html) — 发布于 未知：当Kiro在IDE里帮你写代码，AgentCore在企业平台跑Agent，Quick在桌面替你执行任务——这些"外挂"背
- [NSA 局长亲述：Anthropic Mythos 在数小时内攻破几乎所有机密系统](https://freelamp.com/leisurelinux/articles/0128.html) — 发布于 未知：一条消息在 2026 年 6 月 21 日席卷了全球科技圈：美国 NSA 局长约书亚·鲁德（Joshua
- [Apple 端侧 AI 闪存路由架构：20B 参数不碰 DRAM](https://freelamp.com/leisurelinux/articles/0144.html) — 发布于 2026-06-11 08:58：——AFM3CoreAdvanced如何把200亿参数模型塞进NAND闪存，在iPhone上跑AgentWWDC26上，
- [以封装破制程：华为发布122TB企业级SSD，依靠全新板级封装技术突破外部高层数闪存封锁](https://freelamp.com/leisurelinux/articles/0167.html) — 发布于 2026-05-25 11:03
- [玩转本地大模型（LLM）：拒绝吃灰，5个硬核且实用的落地玩法](https://freelamp.com/leisurelinux/articles/0171.html) — 发布于 未知
- [OpenAI 发布 Daybreak：AI 驱动的防御革命与安全范式转移](https://freelamp.com/leisurelinux/articles/0192.html) — 发布于 未知
- [核心动态：ITSM 市场的“两极化”博弈](https://freelamp.com/leisurelinux/articles/0212.html) — 发布于 2026-05-04 05:13
- [Nebius 收购 Eigen AI：AI 云计算领域的“垂直整合”大动作](https://freelamp.com/leisurelinux/articles/0224.html) — 发布于 2026-05-02 14:21
- [端侧 AI 崛起：手机离线运行大模型的技术解析与行业洞察](https://freelamp.com/leisurelinux/articles/0228.html) — 发布于 2026-05-01 06:17
- [解读 Alphabet 400 亿增资 Anthropic：硅谷进入“无限钱包”博弈时代](https://freelamp.com/leisurelinux/articles/0241.html) — 发布于 未知
- [从UCM技术到VGAA架构：解析华为昇腾970的“制程突围”与AI生态闭环](https://freelamp.com/leisurelinux/articles/0278.html) — 发布于 未知
- [谷歌TPU全栈产品演进与技术架构深度研究报告：面向下一代智能体AI的算力底座](https://freelamp.com/leisurelinux/articles/0280.html) — 发布于 2026-04-25 06:28
- [架构级重构：Nvidia 如何定义 2026 笔记本电脑的“新黄金时代”](https://freelamp.com/leisurelinux/articles/0307.html) — 发布于 2026-04-13 07:58
- [智领编码新范式：通义千问 Qwen3.6-Plus “智能体化”深度解读](https://freelamp.com/leisurelinux/articles/0310.html) — 发布于 未知
- [从对话到进化：Ollama 2026 核心集成的六大 AI 智能体神器](https://freelamp.com/leisurelinux/articles/0312.html) — 发布于 2026-04-10 11:03
- [96.3% 合规率：Gemini API Docs MCP 驱动下的 Agent 自动化工程实践](https://freelamp.com/leisurelinux/articles/0318.html) — 发布于 未知
- [你的手机里到底藏了多少个“大脑”？Google AI 布道师带你拆解黑科技！](https://freelamp.com/leisurelinux/articles/0321.html) — 发布于 未知
- [【译】上下文工程是新的提示工程：拉开 5 万与 50 万美金年薪差距的关键技能](https://freelamp.com/leisurelinux/articles/0336.html) — 发布于 2026-03-27 06:18
- [自主 AI Agent 的系统级革命：深入拆解 OpenClaw 架构与执行逻辑](https://freelamp.com/leisurelinux/articles/0374.html) — 发布于 2026-03-09 14:56
- [追踪技术的 AI 革命：Systing 1.0 正式发布，联动 Claude Code 深度分析 eBPF 数据](https://freelamp.com/leisurelinux/articles/0412.html) — 发布于 未知
- [麦肯锡的黄昏：拆解 Michael Vandi 的 AI 咨询三套 Mega-Prompts](https://freelamp.com/leisurelinux/articles/0546.html) — 发布于 未知
- [2026 金融 AI 架构白皮书：从算力崇拜到意图重构的深度实践（CIO 内参版）](https://freelamp.com/leisurelinux/articles/0579.html) — 发布于 未知：前言：AI 投资的“至暗时刻”与确定性转型进入 2026 年，银行业对大模型的态度已跨越“实验室幻觉”。
- [CISA 要停掉每周漏洞通报：一句「BOD 26-04」说得通吗？](https://freelamp.com/articles/2026-09-19_cisa-ends-vuln-bulletin/): Translation+analysis of CSO Online's report (Maxwell Cooter, 2026-09-18). CISA will stop its weekly known-vulnerability bulletin from 2026-09-28, officially attributing the move to Binding Operational Directive BOD 26-04, which requires federal agencies to prioritize patching by real-world risk factors (evidence of in-the-wild exploitation) rather than severity scores. The article questions why complying with BOD precludes continuing the weekly bulletin — the two are not mutually exclusive, so the real driver is likely resource contraction or the surge in AI-generated threats (CISA warned earlier this month about attackers targeting AI assets). CISA will still publish via KEV, Cybersecurity Alerts and Advisories, and the CVE catalog, and urges CISOs to watch vendors' own security bulletins. Takeaways: the real change is the prioritization pivot from CVSS to in-the-wild exploitation (consistent with KEV); the BOD justification is weak; frontline teams should subscribe KEV into their monitoring and track vendor advisories instead of relying on a weekly email.
  - Key topics: CISA, vulnerability management, BOD 26-04, KEV, vulnerability prioritization, CVSS, security operations, US government
- [Karpathy 编码代理指导原则（Karpathy Guidelines for Coding Agents）](https://freelamp.com/leisurelinux/articles/0594.html) — 发布于 未知
- [零信任安全架构全景解析：从概念到落地的完整指南（2026版）](https://freelamp.com/leisurelinux/articles/0636.html) — 发布于 2026-01-22 11:12

### 安全/漏洞
- [GRR：谷歌开源的远程取证与应急响应框架——Flow / Hunt / osquery 一篇讲清](https://freelamp.com/leisurelinux/articles/0010.html) — 发布于 2026-08-08 10:54：当几百上千台机器同时出问题时，你需要的不是一台台登录，而是一次操作，作用于整个主机群。——FreeLAMP.c
- [一次打穿 OpenAI：libheif 堆溢出 + SSO 缺陷，72 小时接管员工 ChatGPT/Codex 账号](https://freelamp.com/articles/2026-09-19_hacktron-hacking-openai/): Translation+analysis of Hacktron AI's red-team disclosure (Harsh Jaiswal / Mohan Pedhapati / Rahul Maini, 2026-09-13). A 9-step chain — libheif heap buffer overflow (Debian 12/13 missing the security backport, so Discourse's ImageMagick-exposed libheif 1.19.7/1.19.8 was vulnerable) → OpenAI SSO identity flaw → account takeover of employee ChatGPT/Codex accounts from community.openai.com, reaching internal repos within 72 hours. The vulnerable upstream fix was never tagged as a security fix or given a CVE, which is why Debian stable lagged. Exploit authored with Claude Opus 5 (Opus 4.8 couldn't get a reliable ASLR-on exploit; Opus 5 succeeded within hours and an autonomous `/goal` loop reached Discourse Cloud and OpenAI's instance); the broader HEIF Heist campaign (Slack, Meta, GitHub Enterprise, Node frameworks) cost <$3,000 in tokens across three researchers. OpenAI paid a $6,500 bounty (for the OpenAI-side finding only; Discourse fixed via HackerOne, added ImageMagick sandboxing). Takeaways: dependency CVE gaps are real production risk, SSO identity bridges widen blast radius, exploitation cost has collapsed to days via AI, and untrusted image decoding belongs in hardened ephemeral sandboxes (libheif ≥1.23.4).
  - Key topics: OpenAI, SSO misconfiguration, libheif, ImageMagick, Discourse, heap buffer overflow, RCE, community.openai.com, HEIF Heist, Claude Opus 5, bug bounty, supply-chain security, AI security, red team
- [被放弃的 p7zip，终于换上了官方 7-Zip：Debian LTS 的 DLA-4719-1 安全公告解读](https://freelamp.com/leisurelinux/articles/0013.html) — 发布于 2026-08-07 20:11：上游一旦停更，下游要么硬着头皮自己修、要么整体换库——DebianLTS这次选择了后者，直接拥抱官方7-Zip
- [你昨晚没看 `/wp-json/batch/v1`？WordPress 已经替你急了](https://freelamp.com/leisurelinux/articles/0038.html) — 发布于 未知：/wp-json/batch/v1？WordPress已经替你急了-->9.8分。未认证。HTTP调用。默认安装就能跑代
- [SSH 暴力破解深度防御：从攻击链到六层防御体系](https://freelamp.com/leisurelinux/articles/0040.html) — 发布于 2026-07-20 20:12：把SSH当成一道"江湖令牌"——它能保你纵横四海，也能在一夜之间把家门拱手送人。真正可怕的从来不是口令被试出来，而是有人
- [Secure Boot 被破了「14 年中的 13 年」：一个老 shim 的 14 年长寿秘史](https://freelamp.com/leisurelinux/articles/0053.html) — 发布于 未知：微软信任的加密钥匙被人保管了14年，没人换锁；——直到有人拿着钥匙试开了一批大门，大家才发现：哦，原来这是钥匙。ArsT
- [OpenSSH Server 强制启用后量子密码（PQC）实操部署流程](https://freelamp.com/leisurelinux/articles/0077.html) — 发布于 未知：从OpenSSH9.9（2024年10月）到10.4（2026年7月），OpenSSH用两年时间完成了从引入ML-KEM
- [Google 联手 FBI 捣毁 NetNut 家庭网络：200 万台智能电视沦为'肉鸡'的黑暗真相](https://freelamp.com/leisurelinux/articles/0087.html) — 发布于 未知：事件概述2026年7月2日，Google威胁情报组（GTIG）联合FBI、Lumen、Shadowserver等多个机构
- [ISO 27001:2022 认证完全解读 —— 从零到合规的工具箱](https://freelamp.com/leisurelinux/articles/0111.html) — 发布于 未知：后记：本文基于GitHub上的开源项目PehanIn/ISO-27001-2022-Toolkit进行解读，该套件提供了
- [Red Hat 软件包遭篡改：Miasma 蠕虫深度分析与供应链安全启示](https://freelamp.com/leisurelinux/articles/0150.html) — 发布于 2026-06-08 10:08：事件概述2026年6月初，RedHat（红帽）官方npm组织@redhat-cloud-services下的多个软件包被
- [剑指核心：uutils coreutils 0.9.0，Linux 底层江湖的又一波澜](https://freelamp.com/leisurelinux/articles/0158.html) — 发布于 2026-06-04 09:18：剑指核心：uutilscoreutils0.9.0，Linux底层江湖的又一波澜GNUcoreutils这个1984年生
- [软件供应链安全范式变革：PURL元数据标准化、生态系统融合与“SBOM混淆”漏洞深度研究](https://freelamp.com/leisurelinux/articles/0161.html) — 发布于 未知：引言：软件身份识别的演进与PURL的崛起随着针对软件供应链的复杂攻击手段日益增加——如针对XZ Utils后
- [微软发布 Azure Linux 4：从自研“蚕食”到“接管”Fedora 生态，底层逻辑与架构演进深度解构](https://freelamp.com/leisurelinux/articles/0172.html) — 发布于 未知
- [Arm 推进 Linux 内核适配 128 位页表条目扩展 'FEAT_D128'](https://freelamp.com/leisurelinux/articles/0184.html) — 发布于 未知
- [关键 cPanel 身份验证漏洞识别 — 立即更新您的服务器](https://freelamp.com/leisurelinux/articles/0227.html) — 发布于 2026-05-01 06:17
- [性能飙升 30%！NGINX 1.30 稳定版发布：原生支持 MPTCP 与 HTTP/2 Backend](https://freelamp.com/leisurelinux/articles/0298.html) — 发布于 2026-04-16 06:18
- [全球统一执行！2029年TLS证书有效期锁死47天](https://freelamp.com/leisurelinux/articles/0347.html) — 发布于 2026-03-17 07:20
- [Mullvad GotaTun 安全审计深度报告：基于 Rust 的 WireGuard 新架构稳吗？](https://freelamp.com/leisurelinux/articles/0359.html) — 发布于 未知
- [核心分析：从“Coruna”套件看数字军火的流转](https://freelamp.com/leisurelinux/articles/0365.html) — 发布于 未知
- [豆瓣市集“配置门”事件深度研报](https://freelamp.com/leisurelinux/articles/0388.html) — 发布于 未知
- [微软开源 LiteBox：基于 Rust 的库操作系统与 LVBS 硬件级安全架构深潜](https://freelamp.com/leisurelinux/articles/0539.html) — 发布于 2026-02-06 09:33
- [离谱给离谱他妈开门！CloudCone 洛杉矶机房“屠城”始末：底层技术复盘与起底](https://freelamp.com/leisurelinux/articles/0573.html) — 发布于 2026-02-01 11:09
- [教科书级烂活：复盘2026开年第一技术惨案，聊聊线程安全与全局变量的“夺命扣”](https://freelamp.com/leisurelinux/articles/0629.html) — 发布于 未知
- [底层搬砖还是高阶架构？K8s 可观测性的这 10 个问题，是 DevOps 薪资的分水岭！](https://freelamp.com/leisurelinux/articles/0645.html) — 发布于 未知
- [深度拆解 GitHub 2026 首场大宕机：为什么有了 AIOps 还是躲不开“资源争用”？](https://freelamp.com/leisurelinux/articles/0667.html) — 发布于 未知

### Linux/运维
- [国产打印机厂商全景：谁在造整机、谁在做主控，A3 与彩色花落谁家](https://freelamp.com/articles/2026-09-24_domestic-printer-vendors-map/): Original industry survey (public sources, no translation; every fact tagged confirmed/inferred/undisclosed). Domestic printer OEMs number 25+: 16 in the Xinchuang laser-printing main lane (Pantum, Deli, HG Technologies/汉光联创, Lanxum/立思辰, TOEC/光电通, GWI/长城信息, Hannto/极印, Unisplendour Hanntu/紫光汉图, Nantian/南天, Cangtian/沧田, Zhongchu/中矗, ICSP/爱胜品, Hengke/恒安捷, Miaohong/渺宏, Gaode Pinchuang, Ningbo Huagao) plus 9 in adjacent categories (Jolimark/映美, Zonewin/中盈, SNBC/新北洋, HPRT/汉印, Xiaozhuan/小篆, WinMage/影源, CATIC/中航, Lenovo Image, Huawei). Three domestic printer-SoC routes are now visible: **Loongson** (LoongArch; 2P0500 released 2023-11-28 as China's first independent-ISA print SoC — heterogeneous big.LITTLE LA364+LA132, 750/400 MHz, 512KB L2, DDR3, <1.8W; 2P0300 2025, 1.2GHz, small lasers; supplied to 12 signed OEMs), **Geehy/极海** (RISC-V T-Head XuanTie cores, HSP2210/2220, captive to Pantum), and **Zhongnuo/众诺北辰 BCV200** (RISC-V, launched 2026-03-14, 800MHz, hardware SCANIF/PRINTIF/ISP accel, 14 OEMs at launch); plus Cangtian's self-developed "Tianxin" chip. Takeaways: (1) printers' SoC supply is now tripolar and RISC-V is becoming the second main line; (2) Loongson 2P0500/2P0300 are officially **A4** parts, so Loongson-based public models are all A4, while A3 capability sits with non-Loongson or undisclosed-controller vendors — A3+color is confirmed in only four makers (Pantum, HG, Lanxum, Zhongchu); (3) an OEM's chip "signature" is plural (HG, Lanxum, Cangtian, TOEC, Hannto, Hengke, Zhongchu, Unisplendour, Zhongying all sit on both the Loongson and Zhongnuo lines), so judging autonomy requires the specific model's SoC + security-evaluation grade, not the vendor name. Microlithography-adjacent policy: from 2025-07-01 the CNCERT/China Information Security Evaluation Center's《安全可靠测评工作指南 V3.0》added laser/inkjet printer main-control chips as one of five evaluation categories; Loongson 2P0500/2P0300 are Grade II (current highest), Geehy KP3701/SN2701 Grade I. Financial Xinchuang: CITIC Bank ~4,000 Loongson-2P0500 printers via Nantian; Deli says it has won central/provincial and ICBC procurement (details undisclosed).
  - Key topics: printer, SoC, Loongson 2P0500, Loongson 2P0300, LoongArch, Geehy, RISC-V, Zhongnuo BCV200, Pantum, HG Technologies, Lanxum, Deli, A3 printer, color laser, Xinchuang, security evaluation, CITIC Bank, supply chain
- [中国排查国有数据中心里的博通交换机：部分站点占比高达 90%，「国产芯片自用」软引导登场](https://freelamp.com/articles/2026-09-23_china-broadcom-switch-survey/): Compiled from Reuters' report citing the Financial Times (2026-09-23; original paywalled — facts drawn from Reuters' same-headline story relayed by Finimize/MarketScreener). Chinese authorities are surveying the use of Broadcom hardware in state-backed data centers as part of a push to boost domestic suppliers and reduce reliance on foreign AI infrastructure. The survey is led by SASAC (the state-assets regulator); FT says Broadcom switches make up as much as 90% of the hardware at some sites; SASAC may follow up with informal guidance urging state-run facilities to reduce Broadcom under a "domestic chips for domestic use" campaign. Reuters could not immediately verify the FT reporting; Broadcom and SASAC did not immediately comment. Takeaways: this is survey + informal guidance, not an outright ban, but state buyers often treat such nudges as approval standards, so Broadcom's design-in gets squeezed at new-project and renewal nodes — and data-center network gear is extremely sticky (monitoring, training, interoperability testing all build around the qualified vendor); the real alarm is concentration (a single foreign vendor at 90%) exposing the network layer that policy focus on chips (Huawei/Ascend/Cambricon) had overlooked, making Broadcom a de-risking target even off any entity list via its Tomahawk/Jericho switch-silicon and VMware-ecosystem dominance; the timing fits AI-infra bottlenecks spilling from single-card compute to the network (all-reduce, north-south traffic), so the probe is the same playbook as domestic-GPU-replacing-Nvidia — AI self-sufficiency must make the whole stack (compute + network + rack) controllable; beneficiaries are Huawei/H3C/Ruijie, at the cost of rebuilding monitoring/ops/interop around them and verifying high-density/ultra-low-latency/network-management parity, with impact surfacing mainly in refresh/expansion cycles — cross-border data-center solution vendors should fold supply-chain nationality into architecture and selection.
  - Key topics: Broadcom, switch, data center, SASAC, domestic substitution, supply-chain security, AI infrastructure, de-risking, Huawei, H3C, Ruijie
- [声卡整个消失了：一行 rm 删掉 .tplg，SOF 驱动找不到文件的全过程](https://freelamp.com/articles/2026-09-23_sof-tplg-missing-no-soundcard/): Hands-on forensic write-up of a laptop that lost its entire sound card — speakers and microphones both gone, Bluetooth audio the only thing still working — on Debian 13 trixie with an XanMod 7.2.7 kernel and an Alder Lake-P HDA controller. Root cause: a single `sudo rm` had deleted `/usr/lib/firmware/intel/sof-tplg/sof-hda-generic-2ch.tplg`. The SOF driver's existence pre-check (`sof_test_topology_file()` calling `firmware_request_nowarn()`) failed, `sof_probe_work` returned `err: -2` (ENOENT), and no `snd_card` was ever registered — `/proc/asound/cards` empty, `aplay -l` reporting `no soundcards found`, PipeWire falling back to an `auto_null` sink, while `lspci -nnk` still showed `sof-audio-pci-intel-tgl` as `Kernel driver in use` (the distinction between "driver never loaded" and "driver probe failed late"). Bluetooth survived because it rides an independent USB / HCI / A2DP path that never touches either the Intel HDA controller or SOF — the failure domain simply did not reach it. The article then dissects the full mechanism of how the kernel locates and reads the file. (1) The filename is assembled in three parts: the base name `sof-hda-generic` comes from `snd_soc_acpi_intel_hda_machines[]`, the `-2ch` suffix is computed at runtime by `hda_machine_select()` in `sound/soc/sof/intel/hda.c` when `tplg_quirk_mask & SND_SOC_ACPI_TPLG_INTEL_DMIC_NUMBER` is set and the DMIC count is read from the BIOS-provided NHLT table (the `DMICs detected in NHLT tables: 2` line in dmesg), and the `intel/sof-tplg` prefix comes from `sof_dev_desc->default_tplg_path[ipc_type]` in `sof-pci-dev.c`. Practical consequence: the `-2ch` suffix is not your choice but the BIOS's — a firmware update changing the NHLT DMIC count to 4 would make the kernel look for `-4ch.tplg` and silently bypass any edits made to the `-2ch` file; `snd_sof_intel_hda_common dmic_num=N` can force an override. (2) SOF probes the file's existence before loading it: `sof_test_topology_file()` calls `firmware_request_nowarn()` and immediately `release_firmware()` if found. On failure it calls `sof_print_missing_firmware_info()`, which is the origin of the easily-misread message block `SOF firmware and/or topology file not found.` / `Supported default profiles` — that code lists every file expected for the IPC type (both `.ri` and `.tplg`), so seeing both listed does not mean both are missing; only the return values of `sof_test_firmware_file()` / `sof_test_topology_file()` decide, which is why the error text hedges with "and/or". (3) `fw_get_filesystem_firmware()` in `drivers/base/firmware_loader/main.c` tries a fixed `fw_path[]` array — the `firmware_class.path=` kernel parameter, `/lib/firmware/updates/$(uname -r)`, `/lib/firmware/updates`, `/lib/firmware/$(uname -r)`, `/lib/firmware` (note `/lib` is a symlink to `/usr/lib` on this system) — each retried with the empty, `.zst`, then `.xz` suffix, plus `firmware_fallback_platform()` for a UEFI-embedded copy: 15 candidates in strict order, `.zst` before `.xz`. Reads use `kernel_read_file_from_path_initns()`, i.e. with kernel credentials inside the init mount namespace, which is why granting a user read permission is useless and why no chroot or relogin is needed after fixing the file. (4) Measured `.tplg` structure: a 36-byte `struct snd_soc_tplg_hdr` (magic / abi / version / type / size / vendor_type / payload_size / index / count) followed by a variable-length payload, repeated. The verified file is 44247 bytes, 27 blocks, all with ABI 5, closing exactly at 43815 + 396 + 36 = 44247 — no residual bytes. Magic `0x41536F43` lands in the byte stream little-endian as ASCII `CoSA`, i.e. `ASoC` reversed. Block types present: 10 DAPM_GRAPH, 14 DAPM_WIDGET, 1 PCM (7 PCMs), 1 MANIFEST, 1 BACKEND_LINK (6 links). The kernel parser `snd_soc_tplg_component_load()` in `sound/soc/soc-topology.c` is not a single sequential pass but makes eight passes (MANIFEST, VENDOR, CONTROL, WIDGET, PCM_DAI, GRAPH, BE_DAI, LINK) because blocks are not guaranteed to be ordered. (5) ABI validation compares only the MAJOR number (`SOF_ABI_VERSION_INCOMPATIBLE` in `include/uapi/sound/sof/abi.h`), so `Topology: ABI 3:22:1 Kernel ABI 3:23:1` is normal backward compatibility (firmware package 2025.01 against a 2026 kernel); only a MAJOR mismatch is fatal, and fixing that requires a different `firmware-sof-signed` release. The package version determines file contents, never the filename — the name is computed by the kernel. (6) Load ordering explains the counter-intuitive symptom: `snd_sof_probe` → `sof_machine_check` → `sof_select_ipc_and_paths` (where the pre-check fails) → load `sof-adl.ri` → `snd_sof_run_firmware` boots the DSP → register the component → `snd_sof_machine_register` → only then `snd_soc_tplg_component_load()`. The topology is not part of the firmware, so a missing `.tplg` never stops the DSP from booting; it prevents the `snd_card` from being created, surfacing as `error: sof_probe_work failed err: -2`. Fix requires no reboot: restore the file and hot-reload via `echo 0000:00:1f.3 > /sys/bus/pci/drivers/sof-audio-pci-intel-tgl/unbind` then `.../bind`. The reason the reload is clean is that the failed probe leaves the device at `SOF_FW_BOOT_NOT_STARTED` (`sof_set_fw_state` at the end of the error path), so `snd_sof_device_remove()` takes the lightweight branch and never touches the DSP power domain or the i915 wakeref — a device that never succeeded is the safest to reload, whereas a device in `BOOT_COMPLETE` triggers the full teardown path that historically had a wakeref leak (fixed by the "Add remove_late() to sof_init_environment failure path" patch). Because `CONFIG_SND_SOC_SOF_PROBE_WORK_QUEUE=y`, probe runs in `sof_probe_work` asynchronously, so the `bind` write returning does not mean probe finished — the only valid verdict is `aplay -l` showing `card 0`. Two traps documented: (a) the `.bak` file sitting next to it was byte-identical to the `-pdm1` variant (`md5 91e6e760a72927deafe158efe29c9572`), because it snapshotted an already-overwritten file — rolling back to it would have traded "no sound card" for "one fewer microphone"; the authoritative test is `dpkg -V firmware-sof-signed` (`missing` means deleted, a `5` checksum mismatch means modified), and the pristine file (`md5 ef04df1833429c628a71a852bc83e4a4`) can be re-extracted with `apt-get download firmware-sof-signed` + `dpkg-deb -x`; (b) "file exists but content is wrong" and "file does not exist" are different failure modes — the former yields devices with wrong sound, the latter fails the pre-check and registers no card at all, which is why repeated topology-swapping attempts were the wrong direction. Also noted: `hda_dsp_hdmi_build_controls: no PCM in topology for HDMI converter 3` is benign noise (the `-2ch` topology has no PCM node for HDMI converter 3 while the ALC256 codec reports three HDMI converters) and must not be used as a reason to change topologies.
  - Key topics: SOF, Sound Open Firmware, ALSA, ASoC, `.tplg` topology, `sof-hda-generic-2ch.tplg`, firmware loader, `fw_path[]` search order, `.zst`/`.xz` decompression, `kernel_read_file_from_path_initns`, `snd_soc_tplg_hdr`, `SND_SOC_TPLG_MAGIC` (CoSA), `snd_soc_tplg_component_load` eight-pass parsing, `SOF_ABI_VERSION_INCOMPATIBLE` MAJOR-only check, NHLT table, `SND_SOC_ACPI_TPLG_INTEL_DMIC_NUMBER`, `hda_machine_select`, `sof_probe_work`, `CONFIG_SND_SOC_SOF_PROBE_WORK_QUEUE`, sysfs driver bind/unbind hot reload, `dpkg -V`, PipeWire `auto_null`, Debian 13 trixie, XanMod kernel
  - Diagnostic commands: `aplay -l`, `arecord -l`, `cat /proc/asound/cards`, `lspci -nnk -s 00:1f.3`, `journalctl -k -b | grep -E 'sof|snd_hda'`, `dpkg -V firmware-sof-signed`, `dpkg-deb -x firmware-sof-signed_*.deb /tmp/extract`, `md5sum /usr/lib/firmware/intel/sof-tplg/sof-hda-generic-2ch.tplg`, `ls /sys/module/snd_sof/parameters/`, `echo 0000:00:1f.3 > /sys/bus/pci/drivers/sof-audio-pci-intel-tgl/unbind`, `wpctl status`
- [DMS 换 Noctalia：niri 桌面壳迁移实录，以及两者的真实差别](https://freelamp.com/articles/2026-09-22_dms-to-noctalia-niri-shell/): Hands-on migration record moving the desktop shell on Debian 13 + niri 26.04 from DankMaterialShell (DMS) 1.6 to Noctalia 5.1, plus an architecture comparison and pros/cons of each. Key facts (all verified on the machine): DMS 1.6.0db1 is driven by a statically-linked Go main binary (71 MB, `file` reports go1.26.5 statically linked) with Quickshell 0.3.0 (Qt6/QML, 12 libQt6 deps) as the UI layer; Noctalia 5.1.0 is pure C++ on Wayland + OpenGL ES with zero Qt/GTK dependencies (`ldd | grep -ciE 'qt|gtk'` = 0), a 7.7 MB deb extracting to 26 MB. Noctalia v4 was Quickshell-based but is deprecated (official docs: "v4 (Quickshell) is no longer maintained"); v5 is a full rewrite with separate config, coexisting with DMS. apt install simulation: only 3 new packages (noctalia, noctalia-data, fonts-noctalia-tabler), 0 upgrades, 0 removals, no Conflicts/Replaces/Breaks. Three counter-intuitive pitfalls hit during migration: (1) mako's notification daemon is D-Bus activated (cgroup `dbus-:1.1-org.freedesktop.Notifications@0.service`) via `/usr/share/dbus-1/services/fr.emersion.mako.service`, bypassing systemd, so masking `mako.service` is useless — the fix is a user-level override under `~/.local/share/dbus-1/services` pointing at `/bin/false`; (2) the real owner of `org.freedesktop.Notifications` turned out to be DMS itself (PID 71185), not mako, so query `GetNameOwner` before deciding what to stop; (3) `/usr/share/noctalia/assets/templates/niri/niri.kdl` contains `{{colors.*}}` mustache placeholders, and since niri's `include` fails hard, `niri validate` aborts with `invalid unknown format` — a static-color file must be generated first. Source-level finding: niri only runs `spawn-at-startup` once at launch (`src/main.rs`); `State::reload_config()` (`src/niri.rs:1546`) contains zero `spawn_at_startup` hits, so switching shells via hot reload requires manually launching the new shell. Noteworthy design difference: DMS auto-generates and overwrites ten `AUTO-GENERATED` files under `~/.config/niri/dms/` and injects an include into `config.kdl` (even overriding user layout values such as `gaps 6` → `gaps 4`), whereas Noctalia is explicitly non-invasive: "does not manage your compositor settings or take over your dotfiles." Post-switch verification: `listening on org.freedesktop.Notifications`, Intel Iris Xe OpenGL ES 3.2 / Mesa 25.0.7, bars generated on both DP-4 and eDP-1, i18n zh-Hans, `logind session lock monitor active` — with zero windows lost throughout (hot reload, no logout). Panel ids must be probed rather than guessed: `noctalia msg panel-open <bad-id>` reveals the available list (clipboard, control-center, launcher, polkit, session, setup-wizard, test, tray-drawer, wallpaper).
  - Key topics: niri, Wayland, Noctalia, DankMaterialShell, DMS, Quickshell, Qt6, C++, OpenGL ES, Debian 13 trixie, desktop shell, D-Bus activation, org.freedesktop.Notifications, mako, ext-session-lock, layer-shell, spawn-at-startup, KDL, mustache template, dotfiles
  - Diagnostic commands: `ldd /usr/bin/noctalia | grep -ciE 'qt|gtk'`, `file /usr/bin/dms`, `niri validate -c ~/.config/niri/config.kdl`, `niri msg action load-config-file`, `noctalia msg status`, `noctalia msg panel-open <id>`, `dbus-send --session --print-reply --dest=org.freedesktop.DBus /org/freedesktop/DBus org.freedesktop.DBus.GetNameOwner string:org.freedesktop.Notifications`
- [从 Junk 文件夹里救回正常邮件：rspamd 误判治理实录](https://freelamp.com/leisurelinux/articles/0012.html) — 发布于 2026-08-07 20:11：“spam”不是一个标签，而是一个需要被证明的指控——每一封合法邮件误入Junk文件夹，都是对SPF/DKIM
- [watch 命令完全指南：别再用 while true 手敲了](https://freelamp.com/leisurelinux/articles/0014.html) — 发布于 未知：别再用whiletrue写死循环了——watch这条被严重低估的命令，能把你的终端变成一台实时监控面板。——基
- [Linux Mint Cinnamon Wayland 接近 X11 成熟度](https://freelamp.com/leisurelinux/articles/0033.html) — 发布于 未知：Wayland的Cinnamon版本已经成为我所用过的最精致、最可靠的版本。LinuxMint已准备好让你不再怀念X11
- [【GitHub 项目】ghgrab ：选个文件就下来，别再让我 clone 整个仓库了](https://freelamp.com/leisurelinux/articles/0046.html) — 发布于 2026-07-19 20:00：我只想拿一个文件，为什么让我clone整个仓库？ghgrab就是来回答这个问题的。——介绍abhixdd/gh
- [AMDGPU DC 启用 CACp：开源 Linux 终于补上的 OLED 省电功能](https://freelamp.com/leisurelinux/articles/0064.html) — 发布于 2026-07-10 18:20："AMD终于给Linux补上了一项Windows早就有的OLED省电特性——CACp。代码2022年就有，只是开关一直没
- [BoxBuddy 深度解析：当 Distrobox 遇上 GUI](https://freelamp.com/leisurelinux/articles/0129.html) — 发布于 未知：一个Linux发行版不够用？试试BoxBuddy+Distrobox，让所有发行版为你服务。━━━━━━━━━前言就在几
- [Bash 武林秘籍：Awesome Bash 到底有多「Awesome」？](https://freelamp.com/leisurelinux/articles/0135.html) — 发布于 未知：一张清单，扒开Linux命令行世界的最强工具箱━━━━━━━━━前两天在GitHub上翻到一个特别有意思的项目，叫awe
- [Linux SLUB 分配器深度拆解：malloc 背后内核在干什么？](https://freelamp.com/leisurelinux/articles/0159.html) — 发布于 2026-06-01 19:46
- [BleachBit 引入 TUI 的技术演进与生产力价值](https://freelamp.com/leisurelinux/articles/0175.html) — 发布于 未知
- [Let's Encrypt 在交叉签名根证书事件后暂停证书签发](https://freelamp.com/leisurelinux/articles/0200.html) — 发布于 未知
- [破局平庸：2026 Linux 桌面巅峰赛，Zorin OS 与 Solus 的“灵魂对决”](https://freelamp.com/leisurelinux/articles/0265.html) — 发布于 2026-04-28 06:00
- [开源圈炸锅了！Debian 如何应对美国/巴西新出的奇葩法律？](https://freelamp.com/leisurelinux/articles/0314.html) — 发布于 2026-04-09 06:18
- [Windows 平台下的 Unix 仿真与原生构建：Cygwin 与 MSYS2 的深度架构博弈](https://freelamp.com/leisurelinux/articles/0335.html) — 发布于 未知
- [突破 IO 瓶颈：Surge 下载引擎的分布式并发架构与多源调度深度拆解](https://freelamp.com/leisurelinux/articles/0363.html) — 发布于 未知
- [GNU coreutils 9.12 发布：修了一个折磨人多年的 -R 竞态，uname 多了结构化输出](https://freelamp.com/articles/2026-09-14_gnu-coreutils-9.12/): GNU coreutils 9.12 (2026-09-14) translation+analysis of the Phoronix report, cross-checked against upstream NEWS. Key changes: a long-standing bug where `chcon`/`chgrp`/`chmod`/`chown`/`du`/`ls` failed on recursive `-R` traversal when files were removed in parallel is fixed; `stat` and `tail` now support FailFS and NULLFS filesystem types (`tail -f` uses inotify for nullfs); `uname -A`/`--all-labeled` prints each field labeled, one per line (parser-friendly vs `uname -a`); `factor` buffer over-read and Solaris-specific fixes. Notes that the big perf numbers actually landed in 9.11 (cat zero-copy ~6x on Power10, wc 256KiB reads ~10% faster, new cut options) and `--with-linux-crypto` was removed (AF_ALG deprecated, slower than OpenSSL); uutils 0.11.0 also added uname -A, confirming drop-in convergence with GNU.
  - Key topics: GNU coreutils, 9.12, uname -A, recursive -R race, TOCTOU, FailFS, NULLFS, cat zero-copy, wc, kernel builds, uutils compatibility
- [Debian 上线「推理门户」：Scaleway 出资，给 Debian 开发者免费跑 DeepSeek V4 Flash / GLM-5.2](https://freelamp.com/articles/2026-09-23_debian-inference-portal/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-23) on the Debian Inference Portal. After Debian voted to allow the responsible use of generative AI, Scaleway-funded Debian Inference Portal launched as a self-service AI/LLM inference service for Debian contributors; initial models are DeepSeek V4 Flash and GLM-5.2; it exposes an API for editors/AI tooling and requires a Debian Salsa account (Developers/Maintainers) for a key; Scaleway sponsors a fixed monthly credit pool with a per-user weekly budget; allowed uses are packaging, bug triage, fixes, tooling, documentation. Takeaways: governance-first then infrastructure (vote on responsible use then ship the tool, hence Salsa-bound identity, weekly budget, contributor-scoped use — steadier than ship-now-fix-later); the model choice signals open-weight, auditable, cost-controlled inference (DeepSeek V4 Flash is the Enclave red-team 11/11-for-$4.65 value monster; GLM-5.2 is the open-weight model Hugging Face used to review the OpenAI attack trace on its own servers); funder is European cloud Scaleway rather than Big Tech, matching Debian's neutral European roots, and a predictable monthly pool + weekly budget beats a one-off GPU donation for a volunteer project; and it removes the hidden "pay for your own API or hit corporate-compliance walls" barrier, turning AI assistance into a project-level public utility — a contrast with the Omarchy/Alibaba "solder Qwen into the desktop default layer" commercial path, same AI-sinks-to-OS/toolchain trend, two governance postures.
  - Key topics: Debian, Inference Portal, Scaleway, DeepSeek V4 Flash, GLM-5.2, Salsa, open-weight, AI public utility, responsible AI use, inference API
- [SPECviewperf 15 终于出 Linux 原生版：x86_64 / AArch64 都有，但只给 OpenGL 4.5](https://freelamp.com/articles/2026-09-22_specviewperf-15-0-1-linux/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-22) on SPECViewPerf 15.0.1's Linux native build. SPEC shipped SPECViewPerf 15 (the industry-standard workstation GPU benchmark) in May 2025 but Windows-only; the Linux build catches up as 15.0.1, a big update over the previous Linux version (SPECViewPerf 2020), shipping for both x86_64 and AArch64. Caveat: Windows 15 added DirectX 12 + Vulkan, but the Linux 15.0.1 build only supports OpenGL 4.5. Takeaways: Linux is no longer second-class for the benchmark itself, but the OpenGL-4.5-only ceiling means cross-OS numbers are not directly comparable and the API surface must be labeled in any comparison; AArch64 is the real win — Arm workstations (Ampere, Fujitsu MONAKA 144-core) and Apple Silicon (Asahi/Linux or VM) finally get a reproducible standard graphics workload; the lag reflects that viewset traces replay real ISV app (Maya/SolidWorks/CATIA/SNX/Creo) render streams whose center of gravity is still Windows, so the professional-graphics ecosystem remains Windows-centric; and it is a no-op for gamers (3DMark/in-game benchmarks exist) but an essential, authoritative validation/regression tool for GPU vendors' Linux driver teams.
  - Key topics: SPECviewperf, workstation GPU benchmark, Linux, OpenGL 4.5, AArch64, x86_64, DirectX 12, Vulkan, CAD/DCC, professional graphics, viewset
- [Linux perf script 命令大改：甩掉内嵌 Python/Perl，3.7 秒变 0.1 秒](https://freelamp.com/articles/2026-09-21_linux-perf-python-module/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-21) on a large Linux perf-subsystem patch series. It removes the embedded Python and Perl scripting in `perf script` in favor of standalone Python scripts using a new C-written Python `perf` module; Google engineer Ian Rogers led the overhaul (assisted by Gemini 3.1 Pro), cutting a script from 3.7s to 0.1s (~37x). 49 patches, ~21k lines, dropping the old libpython code and legacy scripts; still under review on lore.kernel.org, not yet in mainline. Takeaways: the speedup is mostly removing the per-invocation interpreter startup/embedding tax rather than an algorithm change (don't blame Python first when a hosted script is slow — check the host's init cost); it is a kernel-perf change not yet in distros, so current `perf script` is unchanged until it lands upstream and toolchains catch up; and it sits in the same "old foundational tools getting serious maintenance" wave as the gzip 1.15 and coreutils 9.12 fixes, with AI-assisted engineering shown being used to maintain infrastructure (contrast Hacktron/DeepSeek using AI for offense).
  - Key topics: Linux, perf, perf script, Python, C extension, kernel tooling, Ian Rogers, Google, Gemini, profiling
- [Gzip 1.15 发布：修了几个「自诞生起就存在」的 bug，其中一个是文件删除竞态](https://freelamp.com/articles/2026-09-20_gzip-115-bugfixes/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-20) on Gzip 1.15 succeeding 1.14 (April 2025). 100+ commits, mostly bug fixes collected over ~18 months, several bugs present since the project's inception. Fixes (quoting the GNU announcement): a TOCTOU where gzip could delete the wrong file if another process simultaneously renames an ancestor directory of the gzip destination; `gzip -d` no longer rejects PKZIP signatures/local headers/data descriptors found in well-formed streamed zip files; diagnostics now quote filenames with unusual characters; an uninitialized-memory use on some malformed inputs; a buffer overflow when decompressing an `.lzh` file after a `.Z` file; `.lzh` output corruption when an internal bit buffer was not cleared, and corruption from a previous file's decoding table on consecutive `.lzh` decompressions; `--synchronous` failing to sync unreadable parent directories on O_PATH (Linux) / O_SEARCH (FreeBSD) platforms (bug from 1.7); and a temp-file race in `gzexe`/`zdiff`/`znew` on platforms lacking `mktemp`. Also: gzip now follows the locale from the environment (no longer assumes C), and drops support for FreeBSD 4.11, HP-UX 11.00, Minix 3.1.8, and Windows 8.1 via non-UCRT MinGW. Takeaways: these aren't cosmetic — the `.lzh` buffer overflow and uninitialized-memory use are memory-safety issues exploitable via crafted untrusted input, and the TOCTOU matters for any script that uses gzip for cleanup/temp handling; the "since the beginning" bugs show cold legacy decompression paths (`.lzh`/`.Z`) get little audit despite billions of daily calls; as a pure-fix release with no breaking features and retired ancient platforms, 1.15 is a no-reason-to-delay upgrade, especially on multi-tenant / untrusted-input boundary nodes.
  - Key topics: gzip, GNU, compression, 1.15, bug fixes, buffer overflow, uninitialized memory, TOCTOU, data corruption, gzexe, locale, security operations
- [GNU Wget2 2.3 发布：多了 --progress=dot，修了几个内存泄漏](https://freelamp.com/articles/2026-09-21_gnu-wget2-23/): Translation+analysis of Phoronix's report (Michael Larabel, 2026-09-21) on GNU Wget2 2.3, the first Wget2 release of 2026. User-visible change: new `--progress=dot` (text dot-style progress as an alternative to the default thermometer bar). Also improved the build system/config flow, polished docs, and enhanced OS/2 support (ANSI escape codes, dynamic loading, DOS-like path styles, correct symbol exports). Bug fixes: argument handling, filename truncation on over-long names, iframe `srcdoc` support, and several memory leaks. Download via GitLab. Takeaways: Wget2 is GNU's modern rewrite (HTTP/2, multithreaded, GitLab-hosted) and a different line from classic wget 1.x — most distros still ship 1.x, with Wget2 typically present as the `wget2` command or optional package, so "Wget 2.3" does not mean your distro `wget` upgraded; the memory-leak and long-filename-truncation fixes matter for long-running recursive mirrors (-r -l) and cross-filesystem edge cases (ext4 255B / eCryptfs / NTFS).
  - Key topics: GNU, Wget2, download tool, HTTP/2, --progress=dot, memory leak, filename truncation, OS/2, GitLab
- [Ubuntu 26.10 完成 coreutils 的 Rust 化：你天天敲的 ls/cp/rm 底层换引擎了](https://freelamp.com/articles/2026-09-14_ubuntu-2610-rust-coreutils/): Translation+analysis of OMG! Ubuntu (Joey Sneddon, 2026-09-13). Ubuntu 26.10 "Stonking Stingray" finishes migrating coreutils to Rust (uutils); `cp`/`mv`/`rm` were previously held back on GNU versions in 26.04 LTS due to TOCTOU issues in uutils, now resolved upstream. Canonical began "oxidising" the distro in 2025; 25.10 first shipped Rust utils and made sudo-rs default; commissioned a Zellic security audit of uutils before 26.04; donates €40k/year to Trifecta Tech Foundation (funding a Rust NTP rewrite, planned default by 27.10). uutils targets drop-in GNU compatibility, treating any deviation as a bug. Beta this month, stable 2026-10-15.
  - Key topics: Ubuntu 26.10, Rust, uutils, coreutils, TOCTOU, Canonical, sudo-rs, memory safety, NTP
- [Linux 7.4 内核构建或快 36%，增量构建快 70%：AI 找出瓶颈，人写的补丁](https://freelamp.com/articles/2026-09-14_linux-74-faster-builds/): Translation+analysis of Phoronix (Michael Larabel, 2026-09-14). A patch series (v2) by Lorenzo Stoakes (ARM), with AI assistance locating single-threaded bottlenecks, speeds up full allmodconfig kernel builds ~36% and incremental builds ~70%; 2 of 7 original patches already merged, rest rebased with Rust compiler parallel front-end tuning, hoped for Linux 7.4. "AI finds bottlenecks, humans write the patches" workflow.
  - Key topics: Linux 7.4, kernel build, kbuild, performance, AI-assisted, Rust front-end, Lorenzo Stoakes
- [把 eBPF 安全代理的内核 CPU 开销砍掉 90%：靠的是记忆化，不是 AI](https://freelamp.com/articles/2026-09-14_ebpf-memoization-90-percent/): Translation+analysis of nathan naveen's blog (2026-09-11). An eBPF file-open LSM security agent caches per-inode policy decisions in a `BPF_MAP_TYPE_LRU_HASH` (max_entries 10000); key = mntns_id + mount_id + inode (inode numbers only unique per mount tree, mntns prevents cross-namespace reuse), value = access_index (bitmask) + state. Benchmark: same file opened 200,000 times, kernel cycles dropped from 28B to 3.03B (~90%); `is_restricted_filepath`/`path_check_callback` fell to ~0.02% of stack (perf cycles:k). Edge case: hardlinks share an inode, so when `i_nlink != 1` the cache is skipped and it falls back to the slow path — correctness over hit rate. Repo: bomfather/agent.
  - Key topics: eBPF, LSM, memoization, BPF_MAP_TYPE_LRU_HASH, inode cache, hardlink, i_nlink, performance, dentry, mount namespace

- [rspamd 把 46% 的合法邮件丢进了垃圾箱：一次从配置文件体系开始的排查](https://freelamp.com/articles/2026-09-23_rspamd-false-positive-tuning/): Hands-on false-positive audit of a self-hosted mail gateway (Debian 12 + postfix + dovecot + rspamd 4.2.0), with a systematic walkthrough of rspamd's five-layer configuration system (`rspamd.conf` top-level orchestration; `local.d/` per-key merge vs `override.d/` whole-section strict override; `scores.d/` + `groups.conf` symbol weights; `actions.conf` score thresholds; `modules.d/`, `settings.conf`, `rspamd.local.lua`, `maps.d/` as extension points). Measured outcome: legitimate mail misfiled to Junk dropped from 294/643 (46%) to 40/643 (6%), and per-message scan time from ~4136 ms to 611-1236 ms. Five stacked root causes: (1) `/etc/rspamd/options.inc` had been truncated to 25 lines from the packaged 93 (`options.inc.dpkg-dist`), dropping `filters = "chartable,dkim,regexp,fuzzy_check"`. rspamd's own docs state C modules are enabled only via the `filters` attribute and "if no filters attribute is defined, all C modules are disabled"; these are statically linked into `librspamd-server.so` (`nm -D | grep -ci dkim` = 31, `src/plugins/dkim_check.c` present) with no `.lua` file on disk, so nothing is visible in a file listing. Consequence: `R_DKIM_ALLOW`/`NA`/`REJECT` never registered, silently disabling `WHITELIST_DKIM`, `WHITELIST_SPF_DKIM`, `WHITELIST_DMARC`, `TRUSTED_NEWSLETTER_PASS`, `DMARC_CHECK` and `NO_AUTH_SPOOF` — while `configtest` still printed `syntax OK` with only `destination ... is missing` warnings. The same stub also dropped `dns{timeout=1s;sockets=16;retransmits=5}` and `local_addrs`; rspamd.log showed 1304 `query timed out` per day (concentrated on `asn6.rspamd.com` and DNSWL/URIBL zones), suppressing negative signals such as `DWL_DNSWL_HI` (-3.5). (2) `override.d/policies_group.conf` is priority=10 strict override, and its 28-line version omitted all six `R_DKIM_*` weights, yielding `R_DKIM_ALLOW has no score registered` — a case where `local.d/` (per-key merge) was the correct tool and `override.d/` was not. (3) `FORGED_SENDER` (+5.0) fires whenever envelope `MAIL FROM` and header `From` are not the same mailbox (`same_mailbox()` in `forged_recipients.lua`), which is exactly how every ESP works by design: `publishing@email.mckinsey.com` / `bounce@email.mckinsey.com`, `jobs-noreply@linkedin.com` / `m-1but2tww...@bounce.linkedin.com`, `notifications@github.com` / `bounces@sgmail.github.com`, and VERP mailing lists `author@debian.org` / `bounce-debian-list=user=freelamp.com@lists.debian.org`. In a 3687-message archive, 1785 messages had an envelope domain different-but-related to the header domain and every one was legitimate, zero spam; rspamd's built-in `ENVFROM_PRVS` only matches `prvs=`, `btv1==`, `msprvs1=` and had 0 hits in production. Fixed by a custom Lua symbol `RELAY_ENVELOPE` (score -5.0): envelope domain organisationally related to the header domain AND envelope shaped like a relay AND at least one of SPF/DKIM/DMARC passing; offline coverage of legitimate mail reached 98.5% (2426/2463). The authentication condition is load-bearing — without it a genuine unauthenticated spoof whose domains happen to be related would be waved through. (4) A `force_actions` rule named `BLOCK_SPOOFED_LOCAL_DOMAIN` used the expression `FORGED_SENDER & !DMARC_POLICY_ALLOW & !R_SPF_ALLOW` — which contains no local-domain test at all — so it hard-rejected any message whose envelope differed from its From and lacked SPF/DMARC; production showed 32 forced rejects, 22 of them legitimate `lists.debian.org` posts where `freelamp.com` was merely a VERP tag. Replaced by a Lua symbol `SPOOFED_LOCAL_DOMAIN` that genuinely checks the header From domain against `LOCAL_DOMAINS` and requires SPF, DKIM and DMARC all to have failed plus a non-authenticated, non-local origin. Note: `force_actions.lua` ignores an `enable` key (its rules loop reads only `sett.action` and `sett.expression`), verified by configtest, so the block must be commented out rather than disabled. (5) A global Dovecot Sieve script gated Junk filing on `X-Spam: Yes`, but that header is generated by `milter_headers.lua`'s `x-spam-status` routine under the condition `action ~= 'no action' and action ~= 'greylist'`, making its real threshold `add_header = 6` in `actions.conf`, not `reject = 15` — collapsing the entire 6-to-15 band into a binary verdict. Rewritten to test `X-Rspamd-Action` for `reject`/`rewrite subject` or `X-Spamd-Bar` with eight `+` (one per point). Regression methodology and three traps: `dovecot-keywords` is a per-mailbox index map (the same letter means `NonJunk` in INBOX but something else in another folder, so cross-applying one map inverts the labels and flips the conclusion); re-scanning Maildir breaks DKIM body hashes because stored messages use LF rather than the original CRLF, making `R_DKIM_REJECT` an artifact — judge real DKIM verdicts from `rspamd.log` instead; and rspamd re-reads the `X-Spam: Yes` header it wrote on the previous pass, triggering `SPAM_FLAG` for a spurious +5.0, so self-generated headers must be stripped before any rescan. Bottom line: a passing `configtest` does not mean the checks are running — verify with `rspamc counters` that the symbols actually exist.
  - Key topics: rspamd, postfix, dovecot, spam filtering, false positives, mail gateway, DKIM, SPF, DMARC, Sieve, FORGED_SENDER, ESP bounce address, VERP, local.d vs override.d, actions.conf thresholds, rspamd.local.lua, configtest, Debian 12
  - Diagnostic commands: `rspamadm configtest`, `rspamc counters | grep R_DKIM`, `rspamc stat`, `diff /etc/rspamd/options.inc /etc/rspamd/options.inc.dpkg-dist`, `nm -D /usr/lib/rspamd/librspamd-server.so | grep -ci dkim`, `wc -l /etc/rspamd/{scores.d,override.d}/policies_group.conf`, `journalctl -u opendkim -u rspamd`, `sieve-test <script> <message.eml`, `doveadm fetch`, `cat /path/dovecot-keywords`

### 开源/工具
- [C++ 内存排查四件套：ASan / Valgrind / gdb / memleak 架构师级攻略](https://freelamp.com/leisurelinux/articles/0055.html) — 发布于 未知：内存问题只有两种：一种把你炸得粉碎，一种让你慢慢失血。前者好治，后者才是真正的工程难题。——从架构师视角看C++内存排查
- [瀑布模型的荒诞起源：软件工程史上最大的误会](https://freelamp.com/leisurelinux/articles/0131.html) — 发布于 2026-06-21 15:41：一个从未被作者本人主张过的方法论，怎么就成了整个行业的"圣经"？━━━━━━━━━######缘起：1970年的一篇论文
- [别再瞎布 485 线了！从原理到施工，一篇真正讲明白的 485 总线干货](https://freelamp.com/leisurelinux/articles/0424.html) — 发布于 未知

- [长鑫（CXMT）要在北京做 3D NAND：DRAM 一哥，开始踩 YMTC 的 NAND 地盘](https://freelamp.com/articles/2026-09-18_cxmt-3d-nand-rd-beijing/): Translation+analysis of Tom's Hardware / Reuters reports (2026-09-18) that China's top memory maker ChangXin Memory Technologies (CXMT) intends to build a 3D NAND R&D production line at its second manufacturing facility near Beijing, leaning on a local Beijing research institute whose remit includes NAND development. The line is R&D + pilot (not a commercial mass-production fab), with start timing and whether pilot work graduates to volume manufacturing still undecided — distinct from the separate Beijing DRAM fab expansion discussed in August. Context: a global memory shortage (AI-server demand) expected to keep supply tight through at least 2027. Domestically CXMT leads DRAM while YMTC anchors NAND; a CXMT NAND push would overlap the two "dual stars" of Chinese memory and add a domestic contender alongside Samsung, SK hynix, and Micron.
  - Key topics: CXMT, ChangXin, 3D NAND, YMTC, Yangtze Memory, SSD, DRAM, semiconductor, domestic memory, Beijing fab, memory shortage
- [Git 2.56 发布，3.0 要把 SHA-256 设为默认：reftable、强制 Rust 与大小写 ID 清除](https://freelamp.com/articles/2026-09-18_git-256-and-30/): Translation+analysis of LWN's report (Jonathan Corbet, 2026-09-18) on Git 2.56 (RC, ~700 non-merge commits, ships late September: new `git history drop <commit>` that replays later commits but still refuses merge-containing histories, `git status` suggesting `git pull`, `git refs` create/delete/update/rename, `git branch --delete-merged`, `git add --resolved` that stages only conflict-resolved paths and aborts on leftover conflict markers, config-lock retry; routine fixes/refactors) and the likely Git 3.0 breaking changes. 3.0 candidates: SHA-256 becomes the default hash (replacing SHA-1; non-experimental since 2.42/2023; GitLab since 2024, Forgejo yes, GitHub the last forge gap but a SHA-256 repo was demoed at git-merge), reftable becomes the default ref storage (replacing `.git/refs` files, solves Android's 800k-ref inefficiency, libgit2 gained reftable support and defaulted SHA-256 in August), object IDs accept only lowercase (kills `f00f00` vs `F00F00` ambiguity bugs/vulns), and Rust becomes a hard build requirement from 3.0 (no-Rust-toolchain platforms can't upgrade; the Rust-free 2.99.x line gets long-term maintenance). Takeaways: the 3.0 breaking set is a single theme — paying down backward-compat debt (security, scale, maintainability, ambiguity removal); the SHA-256 default's real break point is forges not Git, eased by forward opt-in + full support for old SHA-1/reftable-less repos; forced Rust's blast radius is overstated and the 2.99.x line buys time; and the user-visible 2.56 wins are small commands (`history drop`, `add --resolved`, `branch --delete-merged`), with `history drop` still blocked by merges showing how many real histories carry merges.
  - Key topics: Git, version control, 2.56, 3.0, SHA-256, SHA-1, reftable, Rust, breaking changes, object ID case, LWN, Junio Hamano, brian m. carlson
- [CPO 的下一站，可能是一块玻璃：共封装光学走向「玻璃集成平台」](https://freelamp.com/articles/2026-09-21_cpo-glass-integration-platform/): Translation+analysis of SemiWiki's piece (Moh Kolb, 2026-09-21) on co-packaged optics (CPO). CPO is framed as a proximity problem — move the optical engine next to the switch ASIC/XPU to shorten the electrical path, cut SerDes power, raise bandwidth density — but proximity doesn't eliminate the physical bottleneck, it just changes its coordinates in the system. Core thesis: packaging is no longer just supporting the system, it is defining what system can be built, and the substrate under CPO becomes strategic. Glass as an integration platform: good dimensional stability, low-loss high-frequency electrical routing, smooth surface for fine RDL, engineerable CTE, and much larger integration dimensions than wafer-constrained silicon interposers; because optics is geometrically sensitive, dimensional stability becomes an architectural capability. Further: future glass substrates could participate in both electrical and optical integration (TGV vertical vias + on-substrate optics) becoming an integration plane. The critical gate is TGV (through-glass via) manufacturing — via geometry, glass damage, metallization, copper fill, adhesion, residual stress, cracking, long-term reliability; glass is NOT a thermal material (thermal path must be deliberately engineered); and ecosystem (materials, equipment, TGV, fine RDL, KGD strategy, inspection, multi-source supply) matters more than the first demo. Pairs with the CXMT 3D NAND and Huawei 122TB SSD (board-level packaging) stories as the same "packaging breaks process limits / substrate inversion" narrative — advanced packaging is moving from sidekick to architecture definer.
  - Key topics: semiconductor, advanced packaging, CPO, co-packaged optics, glass substrate, TGV, heterogeneous integration, reftable-like ref storage, EIC/PIC, AI package, SemiWiki

- [Oracle 新一轮裁员：6 点清晨邮件、28 亿美元重组，以及 13% 的年度失血](https://freelamp.com/articles/2026-09-14_oracle-layoffs-2-8b-restructuring/): Translation+analysis of TechTimes UK (2026-09-14). Oracle began a new layoff round sending termination emails at 6 a.m. Monday; FY2026 workforce fell ~21,000 (13%) to ~141,000 before this round. Restructuring plan cost raised by $700M to ~$2.8B (severance, contract termination, other costs). Reddit accounts describe a coordinated timeline: federated logins disabled 4 a.m. ET, Slack cut 5–5:30 a.m., emails at 6 a.m. sharp. Email text quoted ("today is your last working day"). Oracle has not said whether AI is absorbing eliminated roles and made no public comment on the round. Business Standard names Oracle/Amazon/Dell/Uber/PayPal as drivers of 2026 global layoffs.
  - Key topics: Oracle, layoffs, restructuring, $2.8B, workforce reduction, AI investment, TechTimes UK

<!-- LEISURELINUX_WX_END -->

## Quick Reference

### Linux TLS/PKI Commands
```bash
# Import private CA to system trust store (Debian/Ubuntu)
cp internal_ca.crt /usr/local/share/ca-certificates/
update-ca-certificates

# Verify certificate hash index
openssl x509 -in cert.pem -noout -hash

# Check Java trust store
keytool -list -keystore $JAVA_HOME/lib/security/cacerts

# Test TLS with custom CA bundle
curl --cacert /etc/ssl/certs/ca-certificates.crt https://internal.service.local
```

### Network Diagnostics
```bash
# Check DHCPv6 client status
systemctl status dhcp6c
journalctl -u odhcpd -n 50

# Verify IPv6 RA configuration
ip -6 route show default
rdisc6 -1 eth0
```

## Repository

- Source: https://github.com/LeisureLinux/lore
- RSS Feed: https://freelamp.com/rss.xml
- Author: LeisureLinux (Albert Xu)
- License: CC BY-SA 4.0
- Topics: linux-kernel, tls, pki, devsecops, network-security, cve-analysis, sysadmin, infrastructure, ebpf, systemd
